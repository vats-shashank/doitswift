---
import MainLayout from '../../layouts/MainLayout.astro';

const canonical = new URL(Astro.url.pathname, Astro.site).toString();

const faqsForSchema: { question: string; answerPlain: string }[] = [
  {
    question: 'Is Compress PDF free and unlimited?',
    answerPlain:
      'Yes. You can compress as many PDFs as your device can handle with no signup, watermarks, or daily task limits. There is no paid tier hiding features — batch queue, ZIP download, and optional size targets are all included. Practical limits are your RAM, CPU, and patience for very large files. Because processing stays in the browser, there is no server-side quota to enforce.',
  },
  {
    question: 'Are my PDFs uploaded to DoItSwift servers?',
    answerPlain:
      'No. pdf-lib and PDF.js run entirely in your browser; bytes are read with the File API and never sent to DoItSwift. You can disconnect from the internet after the page loads and compression still works for files already in the queue. That behavior is different from most online PDF tools that upload to cloud workers. Clear downloads and close the tab on shared computers if the documents are sensitive.',
  },
  {
    question: 'What is the difference between Light cleanup and Smaller file?',
    answerPlain:
      'Light cleanup rewrites the PDF structure with pdf-lib, clears common metadata, and saves with object streams while copying existing pages — vectors and text stay selectable, but savings are often modest. Smaller file renders each page to a canvas and embeds JPEGs in a new PDF, which can slash size for scans and photos but makes pages raster images. Choose Light cleanup when fidelity and search matter; choose Smaller file when attachment size is the priority. The UI always shows before and after sizes so you can compare.',
  },
  {
    question: 'Will every PDF get smaller?',
    answerPlain:
      'Usually, but not always. Already-lean PDFs may barely shrink in Light cleanup because there is little redundant structure left to remove. In Smaller file mode the output can occasionally be larger than the original if the source was highly optimized or if JPEG settings are too gentle for the content. The results list shows original size, new size, and percent change so you can spot outliers. Keep originals until you have verified the compressed file opens and looks acceptable.',
  },
  {
    question: 'How do optional target size goals work?',
    answerPlain:
      'In Smaller file mode you can optionally set a maximum output size in megabytes or a minimum shrink percentage. The tool may run multiple compression passes, adjusting JPEG quality (and related settings) to approach the goal when possible. If both fields are filled, the max megabytes goal takes precedence over the percentage goal. These targets are ignored in Light cleanup mode because that path does not rasterize pages. Large targets on huge inputs can take noticeably longer.',
  },
  {
    question: 'Can I compress multiple PDFs at once?',
    answerPlain:
      'Yes. Add several PDFs to the queue, choose your mode and strength once, then click Compress PDFs to process the whole batch. Each finished file gets its own download link with a before-and-after size line. When you have more than one success, use Download all as ZIP to fetch compressed-pdfs.zip in one step. You can change options and run again without re-adding files until you clear the queue or results.',
  },
  {
    question: 'What are the output filenames?',
    answerPlain:
      'Each output keeps the original base filename and adds -compressed.pdf before the extension so you do not overwrite your source by accident. If the input lacks a .pdf suffix, the tool still appends the suffix pattern to a sensible stem. Rename after download if your portal requires a specific naming pattern. Keeping the stem makes it easy to match outputs back to originals in a folder.',
  },
  {
    question: 'Do password-protected PDFs work?',
    answerPlain:
      'Encrypted or password-protected PDFs may fail to load in the browser libraries until they are unlocked. Use DoItSwift Unlock PDF at /pdf/unlock-pdf/ when you have the password, or decrypt with a trusted desktop tool, then compress the unlocked copy here. Do not share passwords in screenshots or support tickets. If unlock is not possible, you will need an authorized copy from the document owner.',
  },
  {
    question: 'What about very large PDFs?',
    answerPlain:
      'The interface warns when you add a file over about 50 megabytes because raster mode can be slow and memory-heavy on long, high-resolution documents. Very large jobs may make the tab unresponsive for a while; avoid closing the tab until processing finishes. On low-RAM devices, try fewer files per batch or use Light cleanup first. If the browser crashes, split the PDF with Split PDF and compress parts separately.',
  },
  {
    question: 'Is there a monthly limit or account required?',
    answerPlain:
      'No accounts are required and there is no monthly cap or premium gate. The tool is fully client-side, so there is no server-side metering of tasks. You still need a modern browser with JavaScript enabled and enough local resources for your file sizes. Corporate proxies that block CDN scripts may prevent PDF.js from loading — allowlist the CDN or try another network if raster mode never starts.',
  },
  {
    question: 'How do I compress a PDF to under 1 MB or 2 MB?',
    answerPlain:
      'Use Smaller file mode and set an optional target in the Try to stay under field — enter 1 or 2 for your megabyte target. The compressor adjusts JPEG quality across multiple passes when needed to approach your target. For very large PDFs (20+ MB), you may need Maximum strength alongside the size target. Extremely aggressive settings can reduce text readability, so preview the output before discarding the original.',
  },
  {
    question: 'Does compressing a PDF reduce quality?',
    answerPlain:
      'Light cleanup mode does not intentionally reduce visual quality; it removes redundant data and metadata while preserving the page content model. Smaller file mode converts pages to JPEG images, which is inherently lossy. At Balanced strength, on-screen quality is usually acceptable for everyday documents. At Maximum strength, text may look softer and gradients may band. The quality and sharpness sliders in Custom mode give fine control. Always preview important documents before distribution.',
  },
];

const faqPageLd = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqsForSchema.map((f) => ({
    '@type': 'Question',
    name: f.question,
    acceptedAnswer: { '@type': 'Answer', text: f.answerPlain },
  })),
};

const softwareAppLd = {
  '@context': 'https://schema.org',
  '@type': 'SoftwareApplication',
  name: 'Compress PDF',
  applicationCategory: 'BusinessApplication',
  operatingSystem: 'Web Browser',
  offers: {
    '@type': 'Offer',
    price: '0',
    priceCurrency: 'USD',
  },
  description:
    'Free PDF compressor with two modes — light cleanup for text PDFs and raster compression for scans. Adjustable quality, target file size, batch processing. Browser-based, files never uploaded.',
  url: canonical,
};

const breadcrumbLd = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    {
      '@type': 'ListItem',
      position: 1,
      name: 'Home',
      item: Astro.site ? new URL('/', Astro.site).href : 'https://doitswift.com/',
    },
    {
      '@type': 'ListItem',
      position: 2,
      name: 'PDF Tools',
      item: Astro.site ? new URL('/pdf/', Astro.site).href : 'https://doitswift.com/pdf/',
    },
    { '@type': 'ListItem', position: 3, name: 'Compress PDF', item: canonical },
  ],
};
---

<MainLayout
  title="Compress PDF Online Free — Reduce PDF File Size Without Uploading | DoItSwift"
  description="Free PDF compressor that runs in your browser — your files are never uploaded. Reduce PDF size by up to 90% for scans, or use light cleanup for text-based PDFs. Batch compression, before/after comparison, ZIP download. No signup, no watermarks, no limits."
  keywords="compress pdf, pdf compressor, reduce pdf size, compress pdf online, pdf size reducer, shrink pdf, compress pdf free, reduce pdf file size, pdf compression online, make pdf smaller"
  canonical={canonical}
>
  <script type="application/ld+json" set:html={JSON.stringify(softwareAppLd)} />
  <script type="application/ld+json" set:html={JSON.stringify(faqPageLd)} />
  <script type="application/ld+json" set:html={JSON.stringify(breadcrumbLd)} />
  <script src="https://cdn.jsdelivr.net/npm/pdf-lib@1.17.1/dist/pdf-lib.min.js" is:inline></script>
  <script src="https://cdn.jsdelivr.net/npm/jszip@3.10.1/dist/jszip.min.js" is:inline></script>

  <script type="module" is:inline>
    import * as pdfjsLib from 'https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.0.379/pdf.min.mjs';
    pdfjsLib.GlobalWorkerOptions.workerSrc =
      'https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.0.379/pdf.worker.min.mjs';
    window.__pdfjsCompress = pdfjsLib;
  </script>

  <nav class="breadcrumb container" aria-label="Breadcrumb">
    <a href="/">Home</a> › <a href="/pdf/">PDF Tools</a> › <span>Compress PDF</span>
  </nav>

  <section class="hero">
    <div class="container">
      <div class="badge">🔒 100% Private — Files Never Leave Your Device</div>
      <h1>Compress PDF Online — Reduce File Size Free, No Upload</h1>
      <p class="subtitle">
        Reduce PDF file size directly in your browser — choose Light cleanup for text-based documents (preserves vectors and text selection) or Smaller file mode for scans and photo-heavy PDFs (up to 90% reduction). Set optional size targets, compress multiple PDFs in batch, compare before-and-after sizes, and download individually or as ZIP. Unlike iLovePDF, Smallpdf, and Adobe Acrobat Online, your files are never uploaded to any server — all compression runs locally on your device.
      </p>
      <div class="trust">
        <span><span class="check">✓</span> Batch files</span>
        <span><span class="check">✓</span> Size comparison</span>
        <span><span class="check">✓</span> Works offline</span>
        <span><span class="check">✓</span> No watermarks</span>
      </div>
      <p class="trust-signal">
        Uses pdf-lib and PDF.js (open-source libraries) for browser-based compression. No server uploads, no file retention. Last updated: April 2026.
      </p>
    </div>
  </section>

  <section class="converter-section">
    <div class="container">
      <div id="successBanner" class="banner" style="display:none" role="status" aria-live="polite">
        <span id="bannerText">Ready</span>
      </div>

      <div class="compress-opts">
        <span class="opt-label">1. How should we shrink your PDF?</span>
        <div class="preset-buttons" role="radiogroup" aria-label="Compression strategy">
          <button type="button" class="preset-btn is-selected" data-strategy="structure" role="radio" aria-checked="true">
            Light cleanup
          </button>
          <button type="button" class="preset-btn" data-strategy="raster" role="radio" aria-checked="false">
            Smaller file (photos &amp; scans)
          </button>
        </div>
        <p class="preset-desc" id="strategyDesc">
          Keeps text sharp and selectable — good for forms and reports. The file may only get a little smaller.
        </p>

        <div id="rasterBlock" class="raster-block" style="display: none" hidden>
          <span class="opt-label sub">Strength</span>
          <div class="preset-buttons" role="radiogroup" aria-label="Image compression strength">
            <button type="button" class="preset-btn raster-preset is-selected" data-rpreset="balanced" role="radio" aria-checked="true">
              Balanced
            </button>
            <button type="button" class="preset-btn raster-preset" data-rpreset="strong" role="radio" aria-checked="false">
              Strong
            </button>
            <button type="button" class="preset-btn raster-preset" data-rpreset="max" role="radio" aria-checked="false">
              Maximum
            </button>
            <button type="button" class="preset-btn raster-preset" data-rpreset="custom" role="radio" aria-checked="false">
              Custom
            </button>
          </div>
          <p class="preset-desc" id="rasterPresetDesc">
            Good balance of size and readability — pages redrawn as JPEGs.
          </p>

          <div id="customRaster" class="custom-raster" style="display: none" hidden>
            <div class="slider-row">
              <label for="jpegQuality">Picture quality <span id="jpegVal">72</span>%</label>
              <input type="range" id="jpegQuality" min="40" max="95" value="72" />
            </div>
            <div class="slider-row">
              <label for="rasterScale">Sharpness / detail <span id="scaleVal">1.35</span>×</label>
              <input type="range" id="rasterScale" min="80" max="200" value="135" />
              <p class="field-hint">Left = smaller file, softer. Right = sharper, larger file.</p>
            </div>
          </div>
        </div>

        <div class="target-row">
          <span class="opt-label sub">Optional target (smaller file mode only)</span>
          <div class="target-inputs">
            <label class="target-field">
              <span>Try to stay under (MB)</span>
              <input type="number" id="targetMb" min="0.1" max="500" step="0.1" placeholder="—" />
            </label>
            <span class="target-or">or</span>
            <label class="target-field">
              <span>Try to shrink by at least (%)</span>
              <input type="number" id="targetPct" min="1" max="95" step="1" placeholder="—" />
            </label>
          </div>
          <p class="field-hint" id="targetHint">
            Leave blank to use the strength buttons above. If you fill both, <strong>max MB</strong> is used. Targets can take
            longer.
          </p>
        </div>
      </div>

      <div class="compress-help" id="compressHelp">
        <h3 class="compress-help-title">Quick guide</h3>
        <ol class="compress-help-steps">
          <li><strong>Choose a mode</strong> — Light cleanup is safest for text. “Smaller file” helps most with scanned or photo PDFs.</li>
          <li><strong>Add PDFs</strong> — They stay on your computer until you compress.</li>
          <li><strong>Click Compress PDFs</strong> — Wait for the size comparison, then download.</li>
          <li><strong>Try again?</strong> — Change options and compress again; your files stay in the list until you remove them.</li>
        </ol>
        <p class="compress-help-note">
          <strong>Light cleanup</strong> tidies the file but often saves only a little space.
          <strong>Smaller file</strong> redraws each page as a picture — much smaller for scans, but text may look softer and
          copy/paste can get worse. Skip “smaller file” for important contracts unless you’ve checked the preview.
        </p>
      </div>

      <div class="add-files-block">
        <span class="opt-label">2. Add PDFs</span>
        <div class="dropzone" id="dropzone">
          <div class="dropzone-icon">📄</div>
          <div class="dropzone-text">Drop PDFs here or click to browse</div>
          <div class="dropzone-sub">Nothing runs yet — files are only listed until you press Compress PDFs below.</div>
          <input type="file" id="fileInput" multiple accept=".pdf,application/pdf" />
        </div>

        <div id="fileQueueWrap" class="file-queue-wrap" style="display: none" hidden>
          <div class="file-queue-head">
            <span class="file-queue-title">Ready to compress: <strong id="queueCount">0</strong> file(s)</span>
            <button type="button" class="btn-text" id="clearQueue">Remove all</button>
          </div>
          <ul class="file-queue" id="fileQueue" aria-label="Queued PDF files"></ul>
        </div>

        <div class="compress-run">
          <button type="button" class="btn btn-lg compress-go" id="compressBtn" disabled>
            Compress PDFs
          </button>
          <p class="compress-run-hint" id="compressBtnHint">
            Add at least one PDF, then click here. You can change options above and compress again without re-uploading.
          </p>
        </div>
      </div>

      <div id="resultsHeader" class="results-header" style="display:none">3. Results</div>
      <div class="results" id="results"></div>

      <div class="actions" id="actions" style="display:none">
        <button class="btn btn-lg" id="downloadZip">⬇ Download all as ZIP</button>
        <button class="btn btn-secondary btn-lg" id="clearResults">Clear results</button>
      </div>
    </div>
  </section>

  <article class="compress-edu" aria-label="Guide: PDF compression modes">
    <div class="container compress-edu-inner">
      <section class="compress-edu-block">
        <h2 id="what-compression-means">Understanding PDF compression on this page</h2>
        <p>
          PDFs mix vector text, fonts, embedded images, and metadata. <strong>Light cleanup</strong> rewrites the document
          with pdf-lib, clears common metadata fields, and saves with object streams — <strong>vectors stay vectors</strong>,
          so savings are often small if the file was already lean. <strong>Smaller file</strong> uses PDF.js to render each
          page and embeds JPEGs — often dramatic for scans and photo-heavy PDFs, but pages become images, so text can look
          softer and copy/search may suffer.
        </p>
        <p>
          For a longer privacy-focused guide, read{' '}
          <a href="/blog/how-to-compress-pdf/">how to compress a PDF</a> on the DoItSwift blog.
        </p>
      </section>

      <section class="compress-edu-block">
        <h2 id="modes-table">Light cleanup vs Smaller file</h2>
        <div class="compress-table-wrap">
          <table class="compress-compare-table">
            <caption class="sr-only">Comparison of Light cleanup and Smaller file compression</caption>
            <thead>
              <tr>
                <th scope="col">Mode</th>
                <th scope="col">Best for</th>
                <th scope="col">Trade-off</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td>Light cleanup</td>
                <td>Forms, reports, crisp text</td>
                <td>Often modest file reduction</td>
              </tr>
              <tr>
                <td>Smaller file</td>
                <td>Scans, photos, huge attachments</td>
                <td>Lossy JPEG pages; check output before deleting originals</td>
              </tr>
            </tbody>
          </table>
        </div>
        <p>
          <strong>Optional targets</strong> (max MB or shrink %) apply only in Smaller file mode; they trigger extra passes to
          approach your goal. If both fields are filled, <strong>max MB</strong> wins. Light cleanup ignores those fields.
        </p>
      </section>

      <section class="compress-edu-block">
        <h2 id="how-much-compress">How Much Can You Compress? Typical Results</h2>
        <div class="compress-table-wrap">
          <table class="compress-compare-table compress-results-table">
            <caption class="sr-only">Typical compression results by PDF type and mode</caption>
            <thead>
              <tr>
                <th scope="col">PDF type</th>
                <th scope="col">Original size</th>
                <th scope="col">Light cleanup</th>
                <th scope="col">Smaller file (balanced)</th>
                <th scope="col">Smaller file (maximum)</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td>Text-heavy report (50 pages)</td>
                <td>2-5 MB</td>
                <td>1.5-4 MB (10-30%)</td>
                <td>1-2 MB (40-60%)</td>
                <td>500 KB-1 MB (70-80%)</td>
              </tr>
              <tr>
                <td>Scanned document (20 pages)</td>
                <td>10-30 MB</td>
                <td>9-28 MB (minimal)</td>
                <td>2-5 MB (70-85%)</td>
                <td>1-3 MB (85-95%)</td>
              </tr>
              <tr>
                <td>Photo-heavy presentation</td>
                <td>15-50 MB</td>
                <td>12-45 MB (10-20%)</td>
                <td>3-10 MB (60-80%)</td>
                <td>2-5 MB (80-90%)</td>
              </tr>
              <tr>
                <td>Simple form / letter</td>
                <td>100-500 KB</td>
                <td>80-450 KB (5-15%)</td>
                <td>Similar or slightly larger</td>
                <td>Similar or slightly larger</td>
              </tr>
              <tr>
                <td>Merged multi-source PDF</td>
                <td>5-20 MB</td>
                <td>4-15 MB (15-30%)</td>
                <td>2-5 MB (50-70%)</td>
                <td>1-3 MB (75-85%)</td>
              </tr>
            </tbody>
          </table>
        </div>
        <p>
          <strong>Key insight:</strong> Light cleanup works best when the PDF has redundant metadata, duplicate fonts, or uncompressed object streams. Smaller file mode works best on scans and photo-heavy PDFs where rasterizing to JPEG yields dramatic savings. Already-optimized PDFs may not shrink much with either mode.
        </p>
      </section>

      <section class="compress-edu-block">
        <h2 id="when-compress">When You Need to Compress a PDF</h2>

        <h3>Email attachment limits</h3>
        <p>
          Gmail caps attachments at 25 MB, Outlook at 20 MB, and many corporate email servers at 10 MB. Compress your PDF to fit within these limits instead of using external file-sharing links that may expire or require accounts.
        </p>

        <h3>Government and university portals</h3>
        <p>
          Online submission portals frequently limit uploads to 2-5 MB per file. Passport applications, visa forms, university admissions, job portals, and tax filing systems all impose strict size limits. Compress scanned documents to meet these requirements.
        </p>

        <h3>After merging multiple PDFs</h3>
        <p>
          Merged PDFs accumulate duplicate fonts and metadata from every source file, making the output larger than expected. A 5+3+4 MB merge can produce a 15 MB file instead of 12 MB. Compress after merging for significant savings — typically 30-60% reduction. Use our <a href="/pdf/merge-pdf/">PDF Merger</a> first, then compress the result.
        </p>

        <h3>Website and app uploads</h3>
        <p>
          CMS platforms, e-commerce product listings, and document management systems often have PDF size limits. Compressing PDFs before upload reduces storage costs and improves download speed for end users.
        </p>

        <h3>Archiving and backup</h3>
        <p>
          Compressing PDFs before archiving saves significant storage space over time. A company generating 100 PDFs/month at 5 MB each saves 30+ GB/year with even modest compression.
        </p>

        <h3>WhatsApp and messaging</h3>
        <p>
          WhatsApp limits document sharing to 100 MB, but large PDFs are slow to send on mobile data. Compressing scanned documents from 20 MB to 2-3 MB makes sharing instant even on slower connections.
        </p>
      </section>

      <section class="compress-edu-block">
        <h2 id="which-mode">Which Compression Mode Should You Use?</h2>
        <div class="compress-table-wrap">
          <table class="compress-compare-table compress-results-table">
            <caption class="sr-only">Recommended compression mode by document type</caption>
            <thead>
              <tr>
                <th scope="col">Your PDF type</th>
                <th scope="col">Recommended mode</th>
                <th scope="col">Why</th>
              </tr>
            </thead>
            <tbody>
              <tr><td>Contract or legal document</td><td><strong>Light cleanup</strong></td><td>Preserves text selectability and search</td></tr>
              <tr><td>Scanned pages / receipts</td><td><strong>Smaller file (balanced)</strong></td><td>Already images — rasterizing loses nothing</td></tr>
              <tr><td>Photo-heavy report</td><td><strong>Smaller file (balanced)</strong></td><td>Biggest gains on embedded photos</td></tr>
              <tr><td>Tax / government form</td><td><strong>Light cleanup</strong></td><td>Must preserve form fields and text clarity</td></tr>
              <tr><td>University assignment</td><td><strong>Light cleanup first, Smaller file if still too large</strong></td><td>Try preserving quality first</td></tr>
              <tr><td>Portfolio / design work</td><td><strong>Smaller file (custom, high quality)</strong></td><td>Adjust quality slider to balance visuals vs size</td></tr>
              <tr><td>Merged multi-document PDF</td><td><strong>Light cleanup</strong></td><td>Removes duplicate fonts and metadata from merge</td></tr>
              <tr><td>Email attachment too large</td><td><strong>Smaller file + target size</strong></td><td>Set target to match email limit (e.g., 5 MB)</td></tr>
            </tbody>
          </table>
        </div>
        <p>
          <strong>General rule:</strong> Start with Light cleanup. If the result isn't small enough, try Smaller file at Balanced strength. Only use Maximum or set aggressive targets when file size matters more than visual quality.
        </p>
      </section>

      <section class="compress-edu-block">
        <h2 id="browser-compress">How Browser-Based PDF Compression Works</h2>
        <p>
          Most PDF compressors (iLovePDF, Smallpdf, Adobe) upload your documents to cloud servers for processing. Your contracts, financial statements, identity documents, and medical records travel across the internet and are processed on someone else's infrastructure. For sensitive documents, this is a privacy risk.
        </p>
        <p>
          DoItSwift compresses PDFs entirely in your browser:
        </p>

        <h3>Light cleanup mode</h3>
        <ol class="compress-edu-ol">
          <li>The pdf-lib JavaScript library reads your PDF in browser memory</li>
          <li>It rewrites the document structure, removing redundant metadata and optimizing object streams</li>
          <li>Vector text, fonts, and images are preserved — the output is structurally identical but cleaner</li>
          <li>A new, smaller PDF is generated and offered for download</li>
        </ol>

        <h3>Smaller file mode</h3>
        <ol class="compress-edu-ol">
          <li>PDF.js renders each page to an HTML Canvas at your specified quality</li>
          <li>Each rendered page is exported as a JPEG image at the selected compression level</li>
          <li>A new PDF is built from these JPEG pages using pdf-lib</li>
          <li>The result is typically 50-90% smaller but pages are now images (text becomes non-selectable)</li>
        </ol>

        <p>
          <strong>Privacy proof:</strong> Disconnect your internet and try compressing a PDF. It works — because no server communication happens at any point. Server-based tools fail instantly when offline.
        </p>
      </section>

      <section class="compress-edu-block">
        <h2 id="vs-competitors">DoItSwift vs Other PDF Compressors</h2>
        <div class="compress-table-wrap">
          <table class="compress-compare-table compress-results-table">
            <caption class="sr-only">Feature comparison: DoItSwift vs iLovePDF, Smallpdf, Adobe Acrobat Online</caption>
            <thead>
              <tr>
                <th scope="col">Feature</th>
                <th scope="col">DoItSwift</th>
                <th scope="col">iLovePDF</th>
                <th scope="col">Smallpdf</th>
                <th scope="col">Adobe Acrobat Online</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td>Files stay on device</td>
                <td><strong>Yes — never uploaded</strong></td>
                <td>No — server processed</td>
                <td>No — server processed</td>
                <td>No — Adobe cloud</td>
              </tr>
              <tr>
                <td>Compression modes</td>
                <td><strong>2 (Light + Raster with quality control)</strong></td>
                <td>3 levels (no control)</td>
                <td>2 levels</td>
                <td>3 levels</td>
              </tr>
              <tr>
                <td>Quality/sharpness control</td>
                <td><strong>Yes — sliders for both</strong></td>
                <td>No</td>
                <td>No</td>
                <td>No</td>
              </tr>
              <tr>
                <td>Target file size</td>
                <td><strong>Yes — set max MB or min % reduction</strong></td>
                <td>No</td>
                <td>No</td>
                <td>No</td>
              </tr>
              <tr>
                <td>Batch compression</td>
                <td><strong>Yes — unlimited</strong></td>
                <td>Limited (free tier)</td>
                <td>2/day free</td>
                <td>Limited</td>
              </tr>
              <tr>
                <td>Before/after comparison</td>
                <td><strong>Yes — per file</strong></td>
                <td>Yes</td>
                <td>Yes</td>
                <td>Yes</td>
              </tr>
              <tr>
                <td>ZIP download</td>
                <td><strong>Yes</strong></td>
                <td>Yes</td>
                <td>Limited</td>
                <td>No</td>
              </tr>
              <tr>
                <td>Works offline</td>
                <td><strong>Yes</strong></td>
                <td>No</td>
                <td>No</td>
                <td>No</td>
              </tr>
              <tr>
                <td>Free tier limits</td>
                <td><strong>None — fully free</strong></td>
                <td>Daily caps</td>
                <td>2 tasks/day</td>
                <td>Requires login</td>
              </tr>
            </tbody>
          </table>
        </div>
        <p>
          <strong>Where DoItSwift wins:</strong> Privacy (no upload), quality control (adjustable sliders), target file size, and unlimited batch processing. No other popular compressor offers all four simultaneously.
        </p>
        <p>
          <strong>Where others win:</strong> Server-based tools handle extremely large files (100+ MB) faster and can apply advanced compression algorithms that aren't available in browser JavaScript. Adobe Acrobat also preserves PDF/A compliance and metadata more reliably. For everyday compression of documents under 50 MB, DoItSwift is faster, more private, and offers more control.
        </p>
      </section>

      <section class="compress-edu-block">
        <h2 id="workflow-next-steps">Split, merge, or export images</h2>
        <p>
          Still too big? Try <a href="/pdf/split-pdf/">Split PDF</a> and compress parts separately, or{' '}
          <a href="/pdf/merge-pdf/">Merge PDF</a> after shrinking pieces. To get pictures out of a PDF, use{' '}
          <a href="/pdf/pdf-to-jpg/">PDF to JPG</a>.
        </p>
      </section>
    </div>
  </article>

  <section class="compress-related" aria-label="Related PDF tools and guides">
    <div class="container">
      <h2 class="section-title">Related tools</h2>
      <ul class="compress-related-grid">
        <li><a href="/pdf/split-pdf/">Split PDF</a></li>
        <li><a href="/pdf/merge-pdf/">Merge PDF</a></li>
        <li><a href="/pdf/unlock-pdf/">Unlock PDF</a></li>
        <li><a href="/pdf/pdf-to-jpg/">PDF to JPG</a></li>
        <li><a href="/pdf/image-to-pdf/">Image to PDF</a></li>
        <li><a href="/pdf/jpg-to-pdf/">JPG to PDF</a></li>
        <li><a href="/image/image-compressor/">Image Compressor — Compress images before embedding in PDFs</a></li>
      </ul>
      <h2 class="section-title section-title-sub">Related guides</h2>
      <ul class="compress-related-grid compress-related-guides">
        <li><a href="/blog/how-to-compress-pdf/">How to compress a PDF</a></li>
        <li><a href="/blog/how-to-merge-pdf/">How to merge PDFs</a></li>
        <li><a href="/blog/how-to-split-pdf/">How to split a PDF</a></li>
        <li><a href="/blog/how-to-convert-pdf-to-jpg/">How to Convert PDF to JPG</a></li>
        <li><a href="/blog/best-image-format-for-web/">Best Image Format for Websites 2026</a></li>
      </ul>
    </div>
  </section>

  <section class="faq">
    <div class="container">
      <h2 class="section-title">Frequently Asked Questions</h2>
      <div class="faq-list">
        <div class="faq-item">
          <h3>Is Compress PDF free and unlimited?</h3>
          <p>
            Yes. You can compress as many PDFs as your device can handle with no signup, watermarks, or daily task limits. There is no paid tier hiding features — batch queue, ZIP download, and optional size targets are all included. Practical limits are your RAM, CPU, and patience for very large files. Because processing stays in the browser, there is no server-side quota to enforce.
          </p>
        </div>
        <div class="faq-item">
          <h3>Are my PDFs uploaded to DoItSwift servers?</h3>
          <p>
            No. pdf-lib and PDF.js run entirely in your browser; bytes are read with the File API and never sent to DoItSwift. You can disconnect from the internet after the page loads and compression still works for files already in the queue. That behavior is different from most online PDF tools that upload to cloud workers. Clear downloads and close the tab on shared computers if the documents are sensitive.
          </p>
        </div>
        <div class="faq-item">
          <h3>What is the difference between Light cleanup and Smaller file?</h3>
          <p>
            Light cleanup rewrites the PDF structure with pdf-lib, clears common metadata, and saves with object streams while copying existing pages — vectors and text stay selectable, but savings are often modest. Smaller file renders each page to a canvas and embeds JPEGs in a new PDF, which can slash size for scans and photos but makes pages raster images. Choose Light cleanup when fidelity and search matter; choose Smaller file when attachment size is the priority. The UI always shows before and after sizes so you can compare.
          </p>
        </div>
        <div class="faq-item">
          <h3>Will every PDF get smaller?</h3>
          <p>
            Usually, but not always. Already-lean PDFs may barely shrink in Light cleanup because there is little redundant structure left to remove. In Smaller file mode the output can occasionally be larger than the original if the source was highly optimized or if JPEG settings are too gentle for the content. The results list shows original size, new size, and percent change so you can spot outliers. Smaller file uses <strong>lossy</strong> JPEG per page. Keep originals until you have verified the compressed file opens and looks acceptable.
          </p>
        </div>
        <div class="faq-item">
          <h3>How do optional target size goals work?</h3>
          <p>
            In Smaller file mode you can optionally set a maximum output size in megabytes or a minimum shrink percentage. The tool may run multiple compression passes, adjusting JPEG quality (and related settings) to approach the goal when possible. If both fields are filled, the max megabytes goal takes precedence over the percentage goal. These targets are ignored in Light cleanup mode because that path does not rasterize pages. Large targets on huge inputs can take noticeably longer.
          </p>
        </div>
        <div class="faq-item">
          <h3>Can I compress multiple PDFs at once?</h3>
          <p>
            Yes. Add several PDFs to the queue, choose your mode and strength once, then click Compress PDFs to process the whole batch. Each finished file gets its own download link with a before-and-after size line. When you have more than one success, use Download all as ZIP to fetch compressed-pdfs.zip in one step. You can change options and run again without re-adding files until you clear the queue or results.
          </p>
        </div>
        <div class="faq-item">
          <h3>What are the output filenames?</h3>
          <p>
            Each output keeps the original base filename and adds <code>-compressed.pdf</code> before the extension so you do not overwrite your source by accident. If the input lacks a .pdf suffix, the tool still appends the suffix pattern to a sensible stem. Rename after download if your portal requires a specific naming pattern. Keeping the stem makes it easy to match outputs back to originals in a folder.
          </p>
        </div>
        <div class="faq-item">
          <h3>Do password-protected PDFs work?</h3>
          <p>
            Encrypted or password-protected PDFs may fail to load in the browser libraries until they are unlocked. Use <a href="/pdf/unlock-pdf/">Unlock PDF</a> when you have the password, or decrypt with a trusted desktop tool, then compress the unlocked copy here. Do not share passwords in screenshots or support tickets. If unlock is not possible, you will need an authorized copy from the document owner.
          </p>
        </div>
        <div class="faq-item">
          <h3>What about very large PDFs?</h3>
          <p>
            The interface warns when you add a file over about 50 megabytes because raster mode can be slow and memory-heavy on long, high-resolution documents. Very large jobs may make the tab unresponsive for a while; avoid closing the tab until processing finishes. On low-RAM devices, try fewer files per batch or use Light cleanup first. If the browser crashes, split the PDF with Split PDF and compress parts separately.
          </p>
        </div>
        <div class="faq-item">
          <h3>Is there a monthly limit or account required?</h3>
          <p>
            No accounts are required and there is no monthly cap or premium gate. The tool is fully client-side, so there is no server-side metering of tasks. You still need a modern browser with JavaScript enabled and enough local resources for your file sizes. Corporate proxies that block CDN scripts may prevent PDF.js from loading — allowlist the CDN or try another network if raster mode never starts.
          </p>
        </div>
        <div class="faq-item">
          <h3>How do I compress a PDF to under 1 MB or 2 MB?</h3>
          <p>
            Use "Smaller file" mode and set an optional target in the "Try to stay under" field — enter 1 or 2 for your MB target. The compressor will adjust JPEG quality automatically across multiple passes to approach your target. For very large PDFs (20+ MB), you may need to use Maximum strength alongside the size target. Note that extremely aggressive compression may reduce text readability — always check the output before discarding the original.
          </p>
        </div>
        <div class="faq-item">
          <h3>Does compressing a PDF reduce quality?</h3>
          <p>
            Light cleanup mode does not reduce visual quality — it only removes redundant data and metadata while preserving the document exactly as-is. Smaller file mode converts pages to JPEG images, which introduces lossy compression. At Balanced strength (default), quality loss is minimal on screen. At Maximum strength, text may appear softer and gradients may show banding. The quality and sharpness sliders give you precise control over this trade-off. For important documents, always preview the compressed output before using it.
          </p>
        </div>
      </div>
    </div>
  </section>

  <script is:inline>
    document.addEventListener('DOMContentLoaded', () => {
      const pdfjsLib = window.__pdfjsCompress;
      const PDFLibGlobal = typeof PDFLib !== 'undefined' ? PDFLib : window.PDFLib;
      if (!PDFLibGlobal) return;
      const { PDFDocument } = PDFLibGlobal;

      const STRATEGY_DESC = {
        structure:
          'Keeps text sharp and selectable — good for forms and reports. The file may only get a little smaller.',
        raster:
          'Redraws each page as a picture (JPEG). Often much smaller for scans and photos; text may look softer — use Light cleanup if you need crisp text.',
      };

      const RASTER_PRESET_DESC = {
        balanced: 'Balanced — smaller files with readable text for most documents.',
        strong: 'Strong — noticeably smaller; best for scans and attachments.',
        max: 'Maximum — smallest files; text and fine lines may look soft.',
        custom: 'Your JPEG quality and detail settings below.',
      };

      const RASTER_DEFAULTS = {
        balanced: { scale: 1.28, jpeg: 0.76 },
        strong: { scale: 1.5, jpeg: 0.6 },
        max: { scale: 1.75, jpeg: 0.46 },
      };

      const strategyBtns = document.querySelectorAll('.preset-btn[data-strategy]');
      const rasterBlock = document.getElementById('rasterBlock');
      const strategyDesc = document.getElementById('strategyDesc');
      const customRaster = document.getElementById('customRaster');
      const rasterPresetDesc = document.getElementById('rasterPresetDesc');
      const jpegSlider = document.getElementById('jpegQuality');
      const scaleSlider = document.getElementById('rasterScale');
      const jpegVal = document.getElementById('jpegVal');
      const scaleVal = document.getElementById('scaleVal');

      strategyBtns.forEach((btn) => {
        btn.addEventListener('click', () => {
          strategyBtns.forEach((b) => {
            b.classList.remove('is-selected');
            b.setAttribute('aria-checked', 'false');
          });
          btn.classList.add('is-selected');
          btn.setAttribute('aria-checked', 'true');
          const st = btn.dataset.strategy;
          strategyDesc.textContent = STRATEGY_DESC[st] || STRATEGY_DESC.structure;
          const show = st === 'raster';
          rasterBlock.style.display = show ? 'block' : 'none';
          rasterBlock.hidden = !show;
        });
      });

      document.querySelectorAll('.raster-preset').forEach((btn) => {
        btn.addEventListener('click', () => {
          document.querySelectorAll('.raster-preset').forEach((b) => {
            b.classList.remove('is-selected');
            b.setAttribute('aria-checked', 'false');
          });
          btn.classList.add('is-selected');
          btn.setAttribute('aria-checked', 'true');
          const rp = btn.dataset.rpreset;
          rasterPresetDesc.textContent = RASTER_PRESET_DESC[rp] || RASTER_PRESET_DESC.balanced;
          const showCust = rp === 'custom';
          customRaster.style.display = showCust ? 'block' : 'none';
          customRaster.hidden = !showCust;
        });
      });

      function syncSliders() {
        if (jpegSlider && jpegVal) jpegVal.textContent = jpegSlider.value;
        if (scaleSlider && scaleVal) scaleVal.textContent = (Number(scaleSlider.value) / 100).toFixed(2);
      }
      jpegSlider?.addEventListener('input', syncSliders);
      scaleSlider?.addEventListener('input', syncSliders);
      syncSliders();

      function getStrategy() {
        const b = document.querySelector('.preset-btn[data-strategy].is-selected');
        return b ? b.dataset.strategy : 'structure';
      }

      function getRasterPreset() {
        const b = document.querySelector('.raster-preset.is-selected');
        return b ? b.dataset.rpreset : 'balanced';
      }

      function getRasterBaseSettings() {
        const rp = getRasterPreset();
        if (rp === 'custom') {
          return {
            scale: Number(scaleSlider.value) / 100,
            jpeg: Number(jpegSlider.value) / 100,
          };
        }
        return RASTER_DEFAULTS[rp] || RASTER_DEFAULTS.balanced;
      }

      /** Target max bytes from MB input, or null */
      function getTargetBytes(origSize) {
        const mbEl = document.getElementById('targetMb');
        const pctEl = document.getElementById('targetPct');
        const mb = parseFloat(mbEl?.value || '');
        const pct = parseFloat(pctEl?.value || '');
        if (!Number.isNaN(mb) && mb > 0) return Math.floor(mb * 1024 * 1024);
        if (!Number.isNaN(pct) && pct > 0 && pct < 100) return Math.floor(origSize * (1 - pct / 100));
        return null;
      }

      function formatBytes(n) {
        if (n >= 1048576) return (n / 1048576).toFixed(2) + ' MB';
        return (n / 1024).toFixed(1) + ' KB';
      }

      /** Structure-only: metadata strip + object streams (vector preserved). */
      async function compressStructureOnly(buf) {
        const src = await PDFDocument.load(buf, { ignoreEncryption: true });
        const out = await PDFDocument.create();
        const pages = await out.copyPages(src, src.getPageIndices());
        pages.forEach((p) => out.addPage(p));
        out.setTitle('');
        out.setAuthor('');
        out.setSubject('');
        return out.save({ useObjectStreams: true, addDefaultPage: false });
      }

      /**
       * Rasterize each page to JPEG. viewportScale multiplies PDF.js default scale (page size in pt).
       * Uses each page's real dimensions (not fixed A4).
       */
      async function compressRaster(buf, pdfjs, viewportScale, jpegQuality) {
        const pdf = await pdfjs.getDocument({ data: buf.slice(0) }).promise;
        const out = await PDFDocument.create();
        const num = pdf.numPages;
        const q = Math.min(0.98, Math.max(0.35, jpegQuality));
        const vs = Math.min(2.25, Math.max(0.65, viewportScale));
        for (let i = 1; i <= num; i++) {
          const page = await pdf.getPage(i);
          const baseVp = page.getViewport({ scale: 1 });
          const pageW = baseVp.width;
          const pageH = baseVp.height;
          const vp = page.getViewport({ scale: vs });
          const canvas = document.createElement('canvas');
          canvas.width = Math.max(1, Math.floor(vp.width));
          canvas.height = Math.max(1, Math.floor(vp.height));
          const ctx = canvas.getContext('2d');
          await page.render({ canvasContext: ctx, viewport: vp }).promise;
          const blob = await new Promise((res) => canvas.toBlob(res, 'image/jpeg', q));
          const jpgBytes = new Uint8Array(await blob.arrayBuffer());
          const img = await out.embedJpg(jpgBytes);
          const pg = out.addPage([pageW, pageH]);
          const s = Math.min(pageW / img.width, pageH / img.height);
          const dw = img.width * s;
          const dh = img.height * s;
          pg.drawImage(img, { x: (pageW - dw) / 2, y: (pageH - dh) / 2, width: dw, height: dh });
        }
        return out.save({ useObjectStreams: true });
      }

      /**
       * Find highest JPEG quality (binary search) so size <= targetBytes at given scale.
       * If impossible at this scale, reduce scale and retry.
       */
      async function compressRasterToTarget(buf, pdfjs, baseScale, targetBytes) {
        let scale = baseScale;
        for (let round = 0; round < 10; round++) {
          let lo = 0.35;
          let hi = 0.95;
          let bestFit = null;
          for (let iter = 0; iter < 15; iter++) {
            const mid = (lo + hi) / 2;
            const out = await compressRaster(buf, pdfjs, scale, mid);
            if (out.byteLength <= targetBytes) {
              bestFit = out;
              lo = mid;
            } else {
              hi = mid;
            }
          }
          if (bestFit) return bestFit;
          scale *= 0.85;
          if (scale < 0.52) break;
        }
        return compressRaster(buf, pdfjs, Math.max(0.55, baseScale * 0.75), 0.38);
      }

      function escapeHtml(s) {
        const d = document.createElement('div');
        d.textContent = s;
        return d.innerHTML;
      }

      const dropzone = document.getElementById('dropzone');
      const fileInput = document.getElementById('fileInput');
      const results = document.getElementById('results');
      const actions = document.getElementById('actions');
      const banner = document.getElementById('successBanner');
      const bannerText = document.getElementById('bannerText');
      const resultsHeader = document.getElementById('resultsHeader');
      const compressBtn = document.getElementById('compressBtn');
      let lastOutputs = [];
      let stagedFiles = [];
      let isCompressing = false;

      function renderQueue() {
        const ul = document.getElementById('fileQueue');
        const wrap = document.getElementById('fileQueueWrap');
        const countEl = document.getElementById('queueCount');
        const hint = document.getElementById('compressBtnHint');
        if (countEl) countEl.textContent = String(stagedFiles.length);
        if (wrap) {
          wrap.style.display = stagedFiles.length ? 'block' : 'none';
          wrap.hidden = stagedFiles.length === 0;
        }
        if (ul) {
          ul.innerHTML = '';
          stagedFiles.forEach((file, i) => {
            const li = document.createElement('li');
            li.className = 'file-queue-item';
            li.innerHTML =
              '<span class="file-queue-name">' +
              escapeHtml(file.name) +
              '</span><span class="file-queue-meta">' +
              formatBytes(file.size) +
              '</span><button type="button" class="btn-text file-queue-remove" data-qi="' +
              i +
              '" aria-label="Remove file from list">Remove</button>';
            ul.appendChild(li);
          });
          ul.querySelectorAll('.file-queue-remove').forEach((b) => {
            b.addEventListener('click', (e) => {
              const idx = Number(e.currentTarget.getAttribute('data-qi'));
              if (!Number.isNaN(idx)) {
                stagedFiles.splice(idx, 1);
                renderQueue();
              }
            });
          });
        }
        if (!isCompressing && compressBtn) {
          compressBtn.disabled = stagedFiles.length === 0;
        }
        if (hint) {
          hint.textContent = stagedFiles.length
            ? 'Click when you’re ready — you can change options above and run again without re-adding files.'
            : 'Add at least one PDF, then click Compress PDFs.';
        }
      }

      function stageFiles(files) {
        const pdfs = files.filter((f) => f.type === 'application/pdf' || /\.pdf$/i.test(f.name));
        if (!pdfs.length) {
          alert('Please choose PDF files only.');
          return;
        }
        for (const f of pdfs) {
          if (f.size > 50 * 1024 * 1024) {
            if (!confirm(f.name + ' is over 50 MB. Add it to the list anyway?')) continue;
          }
          stagedFiles.push(f);
        }
        renderQueue();
      }

      dropzone.addEventListener('click', () => fileInput.click());
      dropzone.addEventListener('dragover', (e) => {
        e.preventDefault();
        dropzone.classList.add('drag');
      });
      dropzone.addEventListener('dragleave', () => dropzone.classList.remove('drag'));
      dropzone.addEventListener('drop', (e) => {
        e.preventDefault();
        dropzone.classList.remove('drag');
        stageFiles(Array.from(e.dataTransfer.files));
      });
      fileInput.addEventListener('change', (e) => {
        const fs = Array.from(e.target.files || []);
        e.target.value = '';
        stageFiles(fs);
      });

      document.getElementById('clearQueue')?.addEventListener('click', () => {
        stagedFiles = [];
        renderQueue();
      });

      compressBtn?.addEventListener('click', () => {
        void runCompression();
      });

      async function runCompression() {
        if (isCompressing) return;
        const pdfs = stagedFiles.slice();
        if (!pdfs.length) {
          alert('Add at least one PDF first (step 2).');
          return;
        }
        let activePdfjs = pdfjsLib;
        const needRaster = getStrategy() === 'raster';
        if (needRaster) {
          for (let i = 0; i < 60; i++) {
            activePdfjs = window.__pdfjsCompress;
            if (activePdfjs) break;
            await new Promise((r) => setTimeout(r, 50));
          }
          if (!activePdfjs) {
            alert('The PDF viewer is still loading. Wait a few seconds and try Compress again.');
            return;
          }
        }
        isCompressing = true;
        if (compressBtn) {
          compressBtn.disabled = true;
          compressBtn.textContent = 'Compressing…';
        }
        banner.style.display = 'block';
        banner.className = 'banner banner-info';
        const strat0 = getStrategy();
        const mb0 = document.getElementById('targetMb')?.value?.trim();
        const pct0 = document.getElementById('targetPct')?.value?.trim();
        const hasGoal = Boolean(mb0 || pct0);
        if (strat0 === 'structure' && hasGoal) {
          bannerText.textContent =
            'ℹ️ Size targets apply to “Smaller file” mode only — using Light cleanup for this run (targets ignored).';
        } else {
          bannerText.textContent = '⏳ Working on your PDFs… This can take a while for large files.';
        }
        try {
        results.innerHTML = '';
        lastOutputs = [];
        for (const file of pdfs) {
          const orig = await file.arrayBuffer();
          const origSize = orig.byteLength;
          let outBytes;
          try {
            const strategy = getStrategy();
            const targetBytes = getTargetBytes(origSize);
            if (strategy === 'structure') {
              outBytes = await compressStructureOnly(orig);
            } else {
              const { scale, jpeg } = getRasterBaseSettings();
              if (targetBytes != null) {
                const trial = await compressRaster(orig, activePdfjs, scale, jpeg);
                if (trial.byteLength <= targetBytes) {
                  outBytes = trial;
                } else {
                  outBytes = await compressRasterToTarget(orig, activePdfjs, scale, targetBytes);
                }
              } else {
                outBytes = await compressRaster(orig, activePdfjs, scale, jpeg);
              }
            }
          } catch (err) {
            const row = document.createElement('div');
            row.className = 'file-item error';
            row.textContent = file.name + ': ' + (err.message || 'failed');
            results.appendChild(row);
            continue;
          }
          const base = file.name.replace(/\.pdf$/i, '') || 'document';
          const name = base + '-compressed.pdf';
          const blob = new Blob([outBytes], { type: 'application/pdf' });
          lastOutputs.push({ name, blob });
          const url = URL.createObjectURL(blob);
          const row = document.createElement('div');
          row.className = 'file-item';
          row.innerHTML =
            '<div class="file-item-main"><div class="file-thumb">🗜️</div><div class="file-info">' +
            '<div class="file-name">' +
            escapeHtml(name) +
            '</div>' +
            '<div class="file-status done">' +
            formatBytes(origSize) +
            ' → ' +
            formatBytes(outBytes.byteLength) +
            ' (' +
            (outBytes.byteLength <= origSize
              ? Math.round((1 - outBytes.byteLength / origSize) * 100) + '% smaller'
              : 'larger than original — try Light cleanup or lower strength') +
            ')</div></div>' +
            '<div class="file-actions-col"><a class="btn" href="' +
            url +
            '" download="' +
            escapeHtml(name) +
            '">Download</a></div></div>';
          results.appendChild(row);
        }
        resultsHeader.style.display = 'block';
        if (lastOutputs.length > 0) {
          actions.style.display = 'flex';
          banner.className = 'banner banner-success';
          bannerText.textContent =
            '✓ Done — your downloads are below. Change options and compress again if needed.';
        } else {
          actions.style.display = 'none';
          banner.className = 'banner banner-info';
          bannerText.textContent = 'No file was compressed successfully — check the error messages above.';
        }
        } catch (err) {
          banner.style.display = 'block';
          banner.className = 'banner banner-info';
          bannerText.textContent = '✗ ' + (err.message || 'Compression failed. Try again or pick a different mode.');
        } finally {
          isCompressing = false;
          if (compressBtn) {
            compressBtn.textContent = 'Compress PDFs';
          }
          renderQueue();
        }
      }


      document.getElementById('downloadZip').addEventListener('click', async () => {
        if (typeof JSZip === 'undefined' || !lastOutputs.length) return;
        const zip = new JSZip();
        lastOutputs.forEach((o) => zip.file(o.name, o.blob));
        const blob = await zip.generateAsync({ type: 'blob' });
        const a = document.createElement('a');
        a.href = URL.createObjectURL(blob);
        a.download = 'compressed-pdfs.zip';
        a.click();
      });

      document.getElementById('clearResults').addEventListener('click', () => {
        results.innerHTML = '';
        lastOutputs = [];
        actions.style.display = 'none';
        banner.style.display = 'none';
        resultsHeader.style.display = 'none';
      });

      renderQueue();

      void (async () => {
        try {
          const pending = await window.DoItSwiftPending?.consume?.();
          if (pending) {
            stageFiles([pending]);
            banner.style.display = 'block';
            banner.className = 'banner banner-info';
            bannerText.textContent = 'File added — adjust options if you like, then click Compress PDFs.';
          }
        } catch (e) {
          console.warn(e);
        }
      })();
    });
  </script>

  <style>
    .breadcrumb {
      font-size: 13px;
      color: var(--text-muted);
      padding: 16px 0 0;
      max-width: 820px;
    }
    .breadcrumb a {
      color: var(--blue);
      text-decoration: none;
    }
    .hero {
      padding: 40px 0 20px;
      text-align: center;
    }
    .subtitle {
      font-size: 19px;
      color: var(--text-muted);
      max-width: 700px;
      margin: 0 auto 20px;
      line-height: 1.6;
    }
    .trust-signal {
      font-size: 12px;
      color: var(--text-muted);
      max-width: 700px;
      margin: 0 auto 20px;
      line-height: 1.55;
    }
    .subtitle strong {
      color: var(--text);
      font-weight: 700;
    }
    .converter-section {
      padding: 30px 0;
    }
    .converter-section .container {
      max-width: 820px;
    }
    .add-files-block {
      margin-top: 8px;
    }
    .add-files-block > .opt-label {
      margin-bottom: 12px;
    }
    .compress-help {
      margin-top: 20px;
      padding: 18px 20px;
      border-radius: var(--radius);
      border: 1px solid var(--border);
      background: var(--bg);
    }
    .compress-help-title {
      font-size: 16px;
      margin: 0 0 12px;
    }
    .compress-help-steps {
      margin: 0 0 14px 20px;
      padding: 0;
      font-size: 14px;
      line-height: 1.55;
      color: var(--text-muted);
    }
    .compress-help-steps li {
      margin-bottom: 8px;
    }
    .compress-help-note {
      font-size: 13px;
      line-height: 1.55;
      color: var(--text-muted);
      margin: 0;
      padding-top: 14px;
      border-top: 1px solid var(--border);
    }
    .file-queue-wrap {
      margin-top: 14px;
    }
    .file-queue-head {
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 8px;
      margin-bottom: 8px;
    }
    .file-queue-title {
      font-size: 14px;
    }
    .file-queue {
      list-style: none;
      margin: 0 0 12px;
      padding: 0;
      border: 1px solid var(--border);
      border-radius: var(--radius-sm);
      background: var(--card);
      max-height: 220px;
      overflow-y: auto;
    }
    .file-queue-item {
      display: flex;
      align-items: center;
      gap: 10px;
      flex-wrap: wrap;
      padding: 10px 12px;
      border-bottom: 1px solid var(--border);
      font-size: 14px;
    }
    .file-queue-item:last-child {
      border-bottom: none;
    }
    .file-queue-name {
      flex: 1;
      min-width: 0;
      font-weight: 600;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
    .file-queue-meta {
      font-size: 13px;
      color: var(--text-muted);
    }
    .btn-text {
      background: none;
      border: none;
      color: var(--blue);
      font-size: 13px;
      font-weight: 600;
      cursor: pointer;
      padding: 4px 8px;
      border-radius: var(--radius-sm);
      font-family: inherit;
    }
    .btn-text:hover {
      text-decoration: underline;
      background: var(--blue-light);
    }
    [data-theme='dark'] .btn-text:hover {
      background: rgba(59, 130, 246, 0.15);
    }
    .compress-run {
      margin-top: 18px;
      text-align: center;
    }
    .compress-go {
      min-width: 240px;
    }
    .compress-go:disabled {
      opacity: 0.55;
      cursor: not-allowed;
    }
    .compress-run-hint {
      font-size: 13px;
      color: var(--text-muted);
      margin: 12px auto 0;
      max-width: 520px;
      line-height: 1.45;
    }
    .banner {
      padding: 14px 20px;
      border-radius: var(--radius-sm);
      margin-bottom: 16px;
      font-weight: 600;
      text-align: center;
    }
    .banner-info {
      background: #dbeafe;
      color: #1e40af;
    }
    .banner-success {
      background: #d1fae5;
      color: #065f46;
    }
    [data-theme='dark'] .banner-info {
      background: rgba(30, 64, 175, 0.2);
      color: #93c5fd;
    }
    [data-theme='dark'] .banner-success {
      background: rgba(6, 95, 70, 0.2);
      color: #6ee7b7;
    }
    .dropzone {
      background: var(--card);
      border: 2px dashed var(--gray-300);
      border-radius: var(--radius-lg);
      padding: 70px 20px;
      text-align: center;
      cursor: pointer;
      box-shadow: var(--shadow);
    }
    .dropzone:hover,
    .dropzone.drag {
      border-color: var(--blue);
      background: var(--blue-light);
    }
    .dropzone-icon {
      font-size: 56px;
      margin-bottom: 14px;
    }
    .dropzone-text {
      font-size: 20px;
      font-weight: 700;
      margin-bottom: 6px;
    }
    .dropzone-sub {
      font-size: 14px;
      color: var(--text-muted);
    }
    #fileInput {
      display: none;
    }
    .compress-opts {
      margin-top: 16px;
      padding: 16px;
      border: 1px solid var(--border);
      border-radius: var(--radius);
      background: var(--card);
    }
    .compress-opts .opt-label {
      display: block;
      font-size: 13px;
      font-weight: 700;
      margin-bottom: 10px;
    }
    .compress-opts .opt-label.sub {
      margin-top: 16px;
    }
    .raster-block {
      margin-top: 14px;
      padding-top: 14px;
      border-top: 1px solid var(--border);
    }
    .target-row {
      margin-top: 16px;
      padding-top: 14px;
      border-top: 1px solid var(--border);
    }
    .target-inputs {
      display: flex;
      flex-wrap: wrap;
      align-items: flex-end;
      gap: 12px 20px;
    }
    .target-field {
      display: flex;
      flex-direction: column;
      gap: 6px;
      font-size: 13px;
      font-weight: 600;
    }
    .target-field input {
      width: 120px;
      max-width: 100%;
      padding: 8px 10px;
      border-radius: var(--radius-sm);
      border: 1px solid var(--border);
      font-size: 14px;
    }
    .target-or {
      font-size: 13px;
      color: var(--text-muted);
      padding-bottom: 8px;
    }
    .field-hint {
      font-size: 12px;
      color: var(--text-muted);
      margin: 6px 0 0;
      line-height: 1.4;
      font-weight: 400;
    }
    .custom-raster {
      margin-top: 12px;
      padding: 12px;
      border-radius: var(--radius-sm);
      background: var(--bg);
      border: 1px solid var(--border);
    }
    .slider-row {
      margin-bottom: 12px;
    }
    .slider-row:last-child {
      margin-bottom: 0;
    }
    .slider-row label {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 12px;
      font-size: 13px;
      font-weight: 600;
      margin-bottom: 6px;
    }
    .slider-row input[type='range'] {
      width: 100%;
    }
    .preset-buttons {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
    }
    .preset-btn {
      flex: 1;
      min-width: 90px;
      padding: 10px 14px;
      border-radius: var(--radius-sm);
      border: 1px solid var(--border);
      background: var(--bg);
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
    }
    .preset-btn.is-selected {
      border-color: var(--blue);
      background: var(--blue-light);
      color: var(--blue);
    }
    .preset-desc {
      font-size: 13px;
      color: var(--text-muted);
      margin: 12px 0 0;
      line-height: 1.45;
    }
    .results-header {
      font-weight: 700;
      margin: 20px 0 10px;
    }
    .file-item {
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 14px;
      margin-bottom: 10px;
      background: var(--card);
    }
    .file-item.error {
      color: #b91c1c;
      font-size: 14px;
    }
    .file-item-main {
      display: flex;
      gap: 12px;
      align-items: center;
    }
    .file-name {
      font-weight: 600;
    }
    .file-status.done {
      font-size: 13px;
      color: var(--text-muted);
    }
    .actions {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-top: 12px;
    }
    .section-title {
      text-align: center;
      margin-bottom: 20px;
    }

    .sr-only {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      white-space: nowrap;
      border: 0;
    }

    .compress-edu {
      padding: 40px 0 16px;
    }
    .compress-edu-inner {
      max-width: 760px;
    }
    .compress-edu-block {
      margin-bottom: 2.5rem;
    }
    .compress-edu-block:last-child {
      margin-bottom: 0;
    }
    .compress-edu h2 {
      font-size: clamp(1.35rem, 2.5vw, 1.55rem);
      line-height: 1.3;
      margin: 0 0 1rem;
      color: var(--text);
    }
    .compress-edu h3 {
      font-size: 1.05rem;
      margin: 1.35rem 0 0.5rem;
      color: var(--text);
      line-height: 1.35;
    }
    .compress-edu-block h2 + h3 {
      margin-top: 0.65rem;
    }
    .compress-edu-ol {
      margin: 0 0 1rem;
      padding-left: 1.35rem;
      font-size: 16px;
      line-height: 1.7;
      color: var(--text);
    }
    .compress-edu-ol li {
      margin-bottom: 0.5rem;
    }
    .compress-edu p {
      font-size: 16px;
      line-height: 1.75;
      color: var(--text);
      margin: 0 0 1rem;
    }
    .compress-edu a {
      color: var(--blue);
      font-weight: 600;
      text-decoration: underline;
      text-underline-offset: 2px;
    }
    .compress-edu a:hover {
      color: var(--blue-dark);
    }
    .compress-edu strong {
      font-weight: 700;
    }
    .compress-table-wrap {
      overflow-x: auto;
      margin: 1rem 0 1.25rem;
      -webkit-overflow-scrolling: touch;
      border-radius: var(--radius);
      border: 1px solid var(--border);
    }
    .compress-compare-table {
      width: 100%;
      min-width: 560px;
      border-collapse: collapse;
      font-size: 14px;
    }
    .compress-results-table {
      min-width: 720px;
    }
    .compress-compare-table th,
    .compress-compare-table td {
      padding: 10px 14px;
      border-bottom: 1px solid var(--border);
      text-align: left;
      vertical-align: top;
    }
    .compress-compare-table thead th {
      background: var(--card);
      font-weight: 700;
      color: var(--text);
    }
    .compress-compare-table tbody tr:last-child td {
      border-bottom: none;
    }

    .compress-related {
      padding: 8px 0 48px;
    }
    .compress-related .container {
      max-width: 820px;
    }
    .section-title-sub {
      margin-top: 2rem;
      margin-bottom: 1rem;
      font-size: 1.2rem;
    }
    .compress-related-grid {
      list-style: none;
      margin: 0;
      padding: 0;
      display: grid;
      gap: 10px;
    }
    @media (min-width: 520px) {
      .compress-related-grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }
    @media (min-width: 720px) {
      .compress-related-grid:not(.compress-related-guides) {
        grid-template-columns: repeat(3, 1fr);
      }
    }
    .compress-related-grid a {
      display: block;
      padding: 14px 16px;
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      color: var(--blue);
      font-weight: 600;
      font-size: 15px;
      text-decoration: none;
      box-shadow: var(--shadow-sm);
      transition: border-color 0.15s ease, background 0.15s ease;
    }
    .compress-related-grid a:hover {
      border-color: var(--blue);
      background: var(--blue-light);
    }
    [data-theme='dark'] .compress-related-grid a:hover {
      background: rgba(0, 102, 255, 0.12);
    }

    .faq {
      padding: 8px 0 60px;
    }
    .faq .container {
      max-width: 820px;
    }
    .faq-item {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 20px 24px;
      margin-bottom: 12px;
      box-shadow: var(--shadow-sm);
    }
    .faq-item h3 {
      font-size: 16px;
      margin-bottom: 8px;
      line-height: 1.35;
      color: var(--text);
    }
    .faq-item p {
      font-size: 14px;
      color: var(--text-muted);
      margin: 0;
      line-height: 1.55;
    }
    .faq-item a {
      color: var(--blue);
      font-weight: 600;
      text-decoration: underline;
      text-underline-offset: 2px;
    }
    .faq-item a:hover {
      color: var(--blue-dark);
    }
    .faq-item code {
      font-size: 0.92em;
      padding: 2px 6px;
      background: var(--gray-100);
      border-radius: 4px;
    }
    [data-theme='dark'] .faq-item code {
      background: rgba(255, 255, 255, 0.08);
    }
    .faq-item strong {
      font-weight: 700;
      color: var(--text);
    }
  </style>
</MainLayout>
