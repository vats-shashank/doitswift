---
import MainLayout from '../../layouts/MainLayout.astro';
import { calculatorCanonical } from '../../lib/calculatorJsonLd';

const canonical = calculatorCanonical(Astro.site, Astro.url.pathname);

const pageDescription =
  'Free period calculator and ovulation tracker. Predict your next period, fertile window, and ovulation day on a visual calendar. 100% browser-based — no app, no account, no data stored.';

const pageKeywords =
  'period calculator, ovulation calculator, period tracker, when is my next period, fertile window calculator, ovulation tracker, menstrual cycle calculator, period predictor, free period calculator, period calculator online';

const faqItems = [
  {
    question: 'Is this a contraception tool?',
    answer:
      'No. This tool is for education and illustration only. Calendar-based rhythm methods and similar approaches have significantly higher failure rates than modern contraception options such as IUDs, implants, injectables, or correctly used hormonal methods. Natural family planning can only be used reliably when taught by qualified instructors and combined with consistent observation. Always discuss contraception with a healthcare professional rather than relying on any online calendar for pregnancy prevention.',
  },
  {
    question: 'How is ovulation estimated?',
    answer:
      'We estimate ovulation using a luteal-phase approach: roughly fourteen days before the start of your next expected period, based on the cycle length you enter. Real ovulation can shift earlier or later because of stress, illness, travel, sleep disruption, or hormonal changes—so treat the marked day as a midpoint estimate, not an exact event. People with irregular cycles may ovulate on very different days from cycle to cycle. For timing that matters clinically, combine calendar estimates with ovulation tests, temperature tracking, or advice from your provider.',
  },
  {
    question: 'What if my cycles are irregular?',
    answer:
      'Predictions from any calendar become less reliable when cycle length varies widely from month to month. Irregular bleeding, very short or very long gaps between periods, or sudden pattern changes deserve attention from a clinician—not guesswork from an app or website. Conditions such as PCOS, thyroid disorders, and others can disturb ovulation timing even when the calendar looks predictable in hindsight. Use this calculator as a rough planning aid only, and seek medical evaluation if bleeding is heavy, painful, or absent without a known cause.',
  },
  {
    question: 'Can this confirm pregnancy?',
    answer:
      'No. Only a pregnancy test performed on urine or blood—or confirmation by a qualified healthcare provider—can diagnose pregnancy. Early pregnancy symptoms overlap with premenstrual symptoms, so calendar delays alone are not proof of conception. Ultrasound dating and serial hCG levels are used clinically when timing matters. If you think you may be pregnant, use an appropriate test and arrange medical follow-up rather than interpreting this tool’s output as a diagnosis.',
  },
  {
    question: 'Does the fertile window guarantee conception timing?',
    answer:
      'No. The fertile window shown here is an educational band based on typical sperm survival and a fixed offset around estimated ovulation. Individual variation in ovulation day, cervical mucus quality, sperm factors, and countless other variables means that conception is never guaranteed on a particular calendar day. The window is useful for general awareness of when pregnancy is biologically more likely, not for precise scheduling. Couples trying to conceive or avoid pregnancy should discuss evidence-based strategies with their providers.',
  },
  {
    question: 'Are cycle dates sent to DoItSwift?',
    answer:
      'No. All arithmetic and date handling for this calculator execute entirely in your web browser using JavaScript on your own device. When you enter your last period and cycle settings, that information never leaves your machine as part of our calculator logic, and we do not store it in a database tied to your visit. Closing the tab clears the session; we do not require an account or login for this page. That design supports privacy-sensitive health use cases more than typical cloud-based period apps might.',
  },
  {
    question: 'How do I calculate when my next period will come?',
    answer:
      'Add your average cycle length to the first day of your last period. For example, if your last period started March 5 and your average cycle is 28 days, your next period is expected around April 2. This calculator does this automatically and shows results on a visual calendar. Track for 3-6 months to find your true average cycle length, since cycles can vary by a few days each month.',
  },
  {
    question: 'How many days after my period do I ovulate?',
    answer:
      'Ovulation typically occurs about 14 days before your next period—not 14 days after your last period. The day count depends on your cycle length. For a 28-day cycle, ovulation is around Day 14 (which happens to be 14 days after the period starts). For a 32-day cycle, ovulation is around Day 18. For a 25-day cycle, it is around Day 11. This calculator estimates your specific ovulation day based on your cycle length.',
  },
  {
    question: 'Can I use this period calculator if I have PCOS or irregular cycles?',
    answer:
      'You can use it for rough estimates, but predictions will be less accurate with irregular cycles. PCOS and other conditions that cause cycle variability mean ovulation may not follow the standard luteal phase pattern. For irregular cycles, combining this calculator with ovulation predictor kits (OPKs) or basal body temperature tracking gives more reliable results. Consult a gynecologist for personalized fertility guidance.',
  },
  {
    question: 'Is this period tracker private? Where is my data stored?',
    answer:
      'Your data is not stored anywhere. This calculator runs entirely in your browser using JavaScript—no information is sent to any server, no account is created, and no cookies track your cycle data. When you close the tab, all data disappears. This makes it one of the most private period tracking options available, unlike apps that store your intimate health data on company servers.',
  },
];

const softwareApplicationLd = {
  '@context': 'https://schema.org',
  '@type': 'SoftwareApplication',
  name: 'Period Calculator & Ovulation Tracker',
  applicationCategory: 'HealthApplication',
  operatingSystem: 'Web Browser',
  offers: {
    '@type': 'Offer',
    price: '0',
    priceCurrency: 'USD',
  },
  description:
    'Free period calculator and ovulation tracker. Predicts next period, fertile window, and ovulation day on a visual calendar. Browser-based, no data stored.',
  url: 'https://doitswift.com/calculator/period-ovulation/',
};

const faqPageLd = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqItems.map((f) => ({
    '@type': 'Question',
    name: f.question,
    acceptedAnswer: {
      '@type': 'Answer',
      text: f.answer,
    },
  })),
};

const breadcrumbLd = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://doitswift.com/' },
    { '@type': 'ListItem', position: 2, name: 'Calculators', item: 'https://doitswift.com/calculator/' },
    {
      '@type': 'ListItem',
      position: 3,
      name: 'Period & Ovulation Calculator',
      item: 'https://doitswift.com/calculator/period-ovulation/',
    },
  ],
};
---

<MainLayout
  title="Free Period Calculator & Ovulation Tracker — Predict Your Next Period | DoItSwift"
  description={pageDescription}
  keywords={pageKeywords}
  canonical={canonical}
>
  <script type="application/ld+json" set:html={JSON.stringify(softwareApplicationLd)} />
  <script type="application/ld+json" set:html={JSON.stringify(faqPageLd)} />
  <script type="application/ld+json" set:html={JSON.stringify(breadcrumbLd)} />

  <nav class="breadcrumb container" aria-label="Breadcrumb">
    <a href="/">Home</a> › <a href="/calculator/">Calculators</a> › <span>Period &amp; Ovulation</span>
  </nav>

  <section class="hero">
    <div class="container">
      <div class="badge">🧮 Free Calculator</div>
      <h1>Period &amp; Ovulation Calculator</h1>
      <p class="subtitle">
        Enter your <strong>last period start</strong> and typical <strong>cycle</strong> and <strong>period length</strong> — we estimate upcoming periods, ovulation, and a fertile window. For comprehensive tracking, apps like Flo or Clue offer more features.
      </p>
      <div class="trust">
        <span><span class="check">✓</span> Private</span>
        <span><span class="check">✓</span> Runs in browser</span>
        <span><span class="check">✓</span> No signup</span>
      </div>
    </div>
  </section>

  <div class="medical-callout container" role="note">
    <strong>⚠️ Important:</strong> This calculator provides <strong>estimates</strong> based on average cycle data. It is <strong>not for contraception</strong> — natural family planning has high failure rates unless taught and monitored professionally. It does <strong>not</strong> confirm pregnancy. Consult a <strong>healthcare provider</strong> for fertility, contraception, irregular cycles, or pregnancy planning. Many women have <strong>irregular cycles</strong>; treat outputs as illustrations only.
    <p class="medical-reviewed">
      Last reviewed: April 2026. Calculations based on standard luteal phase method. For medical advice, consult a qualified healthcare professional.
    </p>
  </div>

  <section class="calc-section">
    <div class="container calc-layout">
      <div class="calc-inputs">
        <h2 class="calc-heading">Inputs</h2>
        <div class="field">
          <label for="poLmp">First day of last period</label>
          <input type="date" id="poLmp" />
        </div>
        <div class="field">
          <label for="poCycle">Average cycle length (days)</label>
          <input type="range" id="poCycleR" min="21" max="45" value="28" aria-hidden="true" />
          <input type="number" id="poCycle" min="21" max="45" value="28" />
        </div>
        <div class="field">
          <label for="poLen">Average period length (days)</label>
          <input type="range" id="poLenR" min="2" max="10" value="5" aria-hidden="true" />
          <input type="number" id="poLen" min="2" max="10" value="5" />
        </div>
        <div class="field">
          <label for="poHorizon">Show next</label>
          <select id="poHorizon" aria-label="Months to calculate">
            <option value="1">1 cycle</option>
            <option value="3" selected>3 cycles</option>
            <option value="6">6 cycles</option>
            <option value="12">12 cycles</option>
          </select>
        </div>
      </div>
      <div class="calc-results" aria-live="polite">
        <h2 class="calc-heading">Cycle calendar</h2>
        <p class="sr-only" id="poCalCap">Color-coded calendar for the selected cycle</p>
        <div class="po-cal-wrap">
          <div class="calendar-legend" aria-hidden="true">
            <span class="legend-item"><span class="legend-dot legend-period"></span> Period</span>
            <span class="legend-item"><span class="legend-dot legend-fertile"></span> Fertile window</span>
            <span class="legend-item"><span class="legend-dot legend-ovulation"></span> Ovulation</span>
          </div>
          <div class="po-nav">
            <button type="button" class="po-nav-btn" id="poPrev" aria-label="Previous cycle" disabled>←</button>
            <div class="po-nav-label" id="poCycleLabel" aria-live="polite">Cycle 1 of 1</div>
            <button type="button" class="po-nav-btn" id="poNext" aria-label="Next cycle">→</button>
          </div>
          <div
            id="poStatusCallout"
            class="cycle-status-callout"
            role="status"
            aria-live="polite"
            hidden
          ></div>
          <div id="poCycleSummary" class="cycle-summary-host"></div>
          <div id="poCalRoot" class="po-cal-root" role="group" aria-labelledby="poCycleLabel poCalCap"></div>
        </div>
        <div class="po-ddp-wrap" id="poDdpWrap">
          <p class="po-ddp-label">Rough due date (this cycle’s ovulation)</p>
          <p class="po-ddp" id="poDdp"></p>
        </div>
      </div>
    </div>
  </section>

  <section class="period-seo-section">
    <div class="container">
      <h2>How to Use This Period Calculator</h2>

      <h3>Step 1: Enter your last period start date</h3>
      <p>
        Select the first day of your most recent menstrual bleeding (not spotting). This is Day 1 of your cycle. If you're unsure, check your calendar or messaging history for clues — many people can recall within a day or two.
      </p>

      <h3>Step 2: Set your average cycle length</h3>
      <p>
        Your cycle length is the number of days from the first day of one period to the first day of the next. The average is 28 days, but anything between 21 and 35 days is considered normal. If you haven't tracked before, start with 28 — you can adjust after tracking for 2-3 months. Use the slider or type directly into the field.
      </p>

      <h3>Step 3: Set your average period length</h3>
      <p>
        This is how many days your period bleeding typically lasts. Most people have periods lasting 3-7 days, with 5 being the average. This determines how many days are highlighted as "period days" on the calendar.
      </p>

      <h3>Step 4: Read your cycle calendar</h3>
      <p>
        The calendar uses color coding to show your predicted cycle: red for period days, green for your fertile window, and a highlighted marker for your estimated ovulation day. Navigate between cycles using the arrows. The calculator also shows a rough due date estimate if conception were to occur during that cycle's ovulation window.
      </p>
    </div>
  </section>

  <section class="period-seo-section">
    <div class="container">
      <h2>Understanding Your Menstrual Cycle</h2>

      <h3>The 4 phases of your menstrual cycle</h3>
      <p>
        Your cycle has four distinct phases that repeat each month. <strong>Menstruation</strong> (Days 1-5 typically) is when the uterine lining sheds — this is your period. The <strong>follicular phase</strong> (Days 1 to ovulation) overlaps with menstruation; your body prepares eggs and estrogen rises. <strong>Ovulation</strong> occurs when a mature egg is released — typically about 14 days before your next period starts. The <strong>luteal phase</strong> (ovulation to next period) lasts almost exactly 12-16 days as progesterone prepares the uterus for potential pregnancy.
      </p>

      <h3>When does ovulation happen?</h3>
      <p>
        Ovulation doesn't always happen on Day 14. It occurs approximately <strong>14 days before your next period starts</strong>, not 14 days after your last period. For a 28-day cycle, that's Day 14. For a 32-day cycle, it's around Day 18. For a 25-day cycle, it's around Day 11. This calculator uses this luteal-phase method to estimate your ovulation date based on your specific cycle length.
      </p>

      <h3>What is the fertile window?</h3>
      <p>
        The fertile window is the approximately 6-day period each cycle when pregnancy is possible: the 5 days before ovulation plus the day of ovulation itself. Sperm can survive in the reproductive tract for up to 5 days, while the egg is viable for only 12-24 hours after release. This is why the fertile window extends well before ovulation day. Our calculator highlights this entire window on the calendar in green.
      </p>

      <h3>How this calculator estimates your dates</h3>
      <p>
        This period calculator uses the <strong>luteal phase method</strong>: it takes your cycle length, subtracts 14 days to estimate ovulation, then marks 5 days before ovulation as the fertile window start. Period days are marked from your last period start date through your period length. Future cycles are projected by adding your cycle length repeatedly. These are estimates — actual ovulation can vary by 1-4 days even in regular cycles.
      </p>
    </div>
  </section>

  <section class="period-seo-section">
    <div class="container">
      <h2>Period Tracking and Privacy</h2>
      <p>
        Period tracking data is deeply personal — it reveals information about fertility, sexual activity, and reproductive health. Most period tracking apps (Flo, Clue, Natural Cycles) store your data on their servers, often in jurisdictions with varying data protection standards.
      </p>
      <p>
        This calculator takes a different approach. <strong>All calculations run entirely in your browser using JavaScript.</strong> Your dates, cycle length, and results are never transmitted to any server. When you close the tab, everything disappears. There is no account, no login, no cloud storage, and no way for us to access your data — because it never leaves your device.
      </p>
      <p>
        You can verify this yourself: load this page, disconnect your internet, and the calculator still works. That's impossible with server-based tools.
      </p>
    </div>
  </section>

  <section class="period-seo-section">
    <div class="container">
      <h2>When to See a Doctor About Your Period</h2>
      <p>
        This calculator is a screening tool, not a medical diagnosis. Consult a healthcare provider if you experience any of the following:
      </p>
      <ul class="period-seo-list">
        <li>Cycles consistently shorter than 21 days or longer than 35 days</li>
        <li>Missed periods for 3 or more consecutive months (without pregnancy)</li>
        <li>Very heavy bleeding (soaking through a pad or tampon every hour for several hours)</li>
        <li>Severe pain that interferes with daily activities</li>
        <li>Bleeding between periods that happens frequently</li>
        <li>Significant change in your established cycle pattern</li>
        <li>Bleeding after menopause</li>
      </ul>
      <p>
        <strong>PCOS awareness:</strong> India has among the highest rates of Polycystic Ovary Syndrome (PCOS) in the world, affecting approximately 1 in 5 women of reproductive age. PCOS causes irregular or absent periods, and calendar-based predictions become unreliable. If you suspect PCOS, consult a gynecologist for proper diagnosis and management.
      </p>
    </div>
  </section>

  <section class="faq">
    <div class="container">
      <h2 class="section-title">Frequently asked questions</h2>
      <div class="faq-list">
        {faqItems.map((f) => (
          <div class="faq-item">
            <h3>{f.question}</h3>
            <p>{f.answer}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section class="related">
    <div class="container">
      <h2 class="section-title">Related Health Calculators</h2>
      <ul class="related-list">
        <li>
          <a href="/calculator/pregnancy-due-date/">Pregnancy Due Date Calculator</a> — Estimate your due date from last period or conception date
        </li>
        <li>
          <a href="/calculator/bmi/">BMI Calculator</a> — Body weight affects cycle regularity and fertility
        </li>
        <li>
          <a href="/calculator/calorie/">Calorie / TDEE Calculator</a> — Nutrition impacts hormonal health and cycle regularity
        </li>
        <li>
          <a href="/calculator/ideal-weight/">Ideal Weight Calculator</a> — Find your healthy weight range for your height
        </li>
        <li>
          <a href="/calculator/body-fat/">Body Fat Calculator</a> — Body composition affects ovulation and fertility
        </li>
        <li>
          <a href="/calculator/water-intake/">Water Intake Calculator</a> — Hydration needs during menstruation
        </li>
        <li>
          <a href="/calculator/age/">Age Calculator</a> — Calculate your exact age
        </li>
      </ul>
    </div>
  </section>

  <section class="related related-guides">
    <div class="container">
      <h2 class="section-title">Related Guides</h2>
      <ul class="related-list">
        <li>
          <a href="/blog/how-to-track-period-cycle/">How to Track Your Period Cycle: Complete Guide</a>
        </li>
        <li>
          <a href="/blog/how-to-calculate-bmi/">How to Calculate Your BMI (Indian Ranges Included)</a>
        </li>
        <li>
          <a href="/blog/how-many-calories-per-day/">How Many Calories Per Day (Indian Food Guide)</a>
        </li>
        <li>
          <a href="/blog/how-much-water-per-day/">How Much Water Should You Drink Per Day?</a>
        </li>
      </ul>
    </div>
  </section>

  <script is:inline>
    (function () {
      var HEADERS = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];
      var currentCycleIdx = 0;
      function pad(n) {
        return n < 10 ? '0' + n : '' + n;
      }
      function fmt(d) {
        return d.getFullYear() + '-' + pad(d.getMonth() + 1) + '-' + pad(d.getDate());
      }
      function fmtLong(d) {
        return d.toLocaleDateString(undefined, { weekday: 'short', month: 'short', day: 'numeric', year: 'numeric' });
      }
      function fmtShortDate(d) {
        return d.toLocaleDateString(undefined, { month: 'short', day: 'numeric', year: 'numeric' });
      }
      function addDays(d, n) {
        var x = new Date(d);
        x.setDate(x.getDate() + n);
        return x;
      }
      function sameDay(a, b) {
        return a.getFullYear() === b.getFullYear() && a.getMonth() === b.getMonth() && a.getDate() === b.getDate();
      }
      function dayOrder(d) {
        return new Date(d.getFullYear(), d.getMonth(), d.getDate()).getTime();
      }
      function isBetween(d, a, b) {
        var t = dayOrder(d);
        return t >= dayOrder(a) && t <= dayOrder(b);
      }
      /** Monday = 0 … Sunday = 6 */
      function mondayFirstIndex(jsDay) {
        return (jsDay + 6) % 7;
      }
      /** First day of the cycle that contains `today` (day 1 = period start). */
      function getCurrentPeriodStart(lmp, cycleLen, today) {
        var t = dayOrder(today);
        var ps = new Date(lmp);
        ps.setHours(12, 0, 0, 0);
        if (t < dayOrder(ps)) return null;
        while (dayOrder(addDays(ps, cycleLen)) <= t) {
          ps = addDays(ps, cycleLen);
        }
        return ps;
      }
      function cycleDayNumber(ps, today) {
        return Math.round((dayOrder(today) - dayOrder(ps)) / 86400000) + 1;
      }
      /** Ovulation on cycle day (C − 14). Menstrual overlaps fertile days → menstrual first. */
      function phaseNameForCycleDay(d, C, P, O) {
        if (d >= 1 && d <= P) return 'Menstrual phase';
        if (d === O) return 'Ovulation day';
        if (d >= O - 5 && d <= O - 1) return 'Fertile window';
        if (d >= O + 1 && d <= C) return 'Luteal phase';
        if (d > P && d < O - 5) return 'Follicular phase';
        return 'Follicular phase';
      }
      function pregnancyChanceForCycleDay(d, C, P, O) {
        if (d >= 1 && d <= P) return 'Low';
        if (d === O) return 'High';
        if (d >= O - 5 && d <= O - 1) return 'Moderate';
        return 'Low';
      }
      /** e.g. Mar 14–18 or Mar 30 – Apr 4 */
      function fmtRangeStat(a, b) {
        if (a.getFullYear() === b.getFullYear() && a.getMonth() === b.getMonth()) {
          var mo = a.toLocaleDateString(undefined, { month: 'short' });
          return mo + ' ' + a.getDate() + '–' + b.getDate();
        }
        return fmtShortDate(a) + ' – ' + fmtShortDate(b);
      }
      function fmtCycleSummary(ci, lmp, cycleLen, plen) {
        var periodStart = addDays(lmp, ci * cycleLen);
        var nextCycleStart = addDays(lmp, (ci + 1) * cycleLen);
        var pEnd = addDays(periodStart, plen - 1);
        var ov = addDays(nextCycleStart, -14);
        var fwS = addDays(ov, -5);
        var fwE = addDays(ov, 1);
        var fertileDayCount = Math.round((dayOrder(fwE) - dayOrder(fwS)) / 86400000) + 1;
        var started = periodStart.toLocaleDateString(undefined, { month: 'short', day: 'numeric', year: 'numeric' });
        var h =
          '<div class="cycle-summary">' +
          '<h3 class="cycle-summary-title">Cycle ' +
          (ci + 1) +
          ' · Started ' +
          started +
          '</h3>' +
          '<div class="cycle-stats">' +
          '<div class="stat">' +
          '<span class="stat-label">Period</span>' +
          '<span class="stat-value">' +
          fmtRangeStat(periodStart, pEnd) +
          '</span>' +
          '<span class="stat-detail">' +
          plen +
          ' days</span></div>' +
          '<div class="stat">' +
          '<span class="stat-label">Fertile window</span>' +
          '<span class="stat-value">' +
          fmtRangeStat(fwS, fwE) +
          '</span>' +
          '<span class="stat-detail">' +
          fertileDayCount +
          ' days</span></div>' +
          '<div class="stat stat-highlight">' +
          '<span class="stat-label">Ovulation</span>' +
          '<span class="stat-value">' +
          fmtShortDate(ov) +
          '</span>' +
          '<span class="stat-detail">Peak fertility</span></div>' +
          '<div class="stat">' +
          '<span class="stat-label">Next period</span>' +
          '<span class="stat-value">' +
          fmtShortDate(nextCycleStart) +
          '</span>' +
          '<span class="stat-detail">Cycle day 1</span></div>' +
          '</div></div>';
        return h;
      }
      function fmtStatusCallout(lmp, cycleLen, plen) {
        var today = new Date();
        today.setHours(12, 0, 0, 0);
        var ps = getCurrentPeriodStart(lmp, cycleLen, today);
        if (!ps) {
          return (
            '<p class="cycle-status-line">📍 Enter the <strong>first day of your last period</strong> (on or before today) to see where you are in your cycle.</p>'
          );
        }
        var C = cycleLen;
        var O = C - 14;
        var d = cycleDayNumber(ps, today);
        if (d < 1) d = 1;
        if (d > C) d = C;
        var phase = phaseNameForCycleDay(d, C, plen, O);
        var chance = pregnancyChanceForCycleDay(d, C, plen, O);
        var nextP = addDays(ps, C);
        var daysUntil = Math.round((dayOrder(nextP) - dayOrder(today)) / 86400000);
        var nextLine;
        if (daysUntil <= 0) {
          nextLine = 'Next period expected: <strong>' + fmtShortDate(nextP) + '</strong>';
        } else {
          nextLine =
            'Next period expected in <strong>' +
            daysUntil +
            '</strong> day' +
            (daysUntil === 1 ? '' : 's') +
            ' (<strong>' +
            fmtShortDate(nextP) +
            '</strong>)';
        }
        return (
          '<p class="cycle-status-line cycle-status-line--main">📍 You are on <strong>Day ' +
          d +
          '</strong> of your cycle (<strong>' +
          phase +
          '</strong>)</p>' +
          '<p class="cycle-status-line">' +
          nextLine +
          '</p>' +
          '<p class="cycle-status-line cycle-status-line--chance">Pregnancy chance today: <strong>' +
          chance +
          '</strong> <span class="cycle-status-hint">(illustrative only)</span></p>'
        );
      }
      /**
       * Ovulation before fertile; period vs ovulation same day → ovulation.
       */
      function cellPhase(cellDate, lmp, cycleLen, plen, ci) {
        var pStart = addDays(lmp, ci * cycleLen);
        var pEnd = addDays(pStart, plen - 1);
        var nextP = addDays(lmp, (ci + 1) * cycleLen);
        var ov = addDays(nextP, -14);
        var fwS = addDays(ov, -5);
        var fwE = addDays(ov, 1);
        if (sameDay(cellDate, ov)) return 'o';
        if (isBetween(cellDate, pStart, pEnd)) return 'p';
        if (isBetween(cellDate, fwS, fwE)) return 'f';
        return 'n';
      }
      function monthsOverlappingCycle(periodStart, lastDayOfCycle) {
        var list = [];
        var y = periodStart.getFullYear();
        var m = periodStart.getMonth();
        var yE = lastDayOfCycle.getFullYear();
        var mE = lastDayOfCycle.getMonth();
        var cy = y;
        var cm = m;
        while (cy < yE || (cy === yE && cm <= mE)) {
          list.push({ year: cy, month: cm });
          cm++;
          if (cm > 11) {
            cm = 0;
            cy++;
          }
        }
        return list;
      }
      /** One month: single .calendar-grid with 7 columns; all cells are direct grid children */
      function buildMonthSection(y, m, lmp, cycleLen, plen, ci) {
        var wrap = document.createElement('div');
        wrap.className = 'calendar-month';
        var title = document.createElement('h3');
        title.className = 'calendar-month-title';
        title.textContent = new Date(y, m, 1).toLocaleDateString(undefined, { month: 'long', year: 'numeric' });
        wrap.appendChild(title);
        var grid = document.createElement('div');
        grid.className = 'calendar-grid';
        var h;
        for (h = 0; h < 7; h++) {
          var hd = document.createElement('div');
          hd.className = 'calendar-day-header';
          hd.textContent = HEADERS[h];
          grid.appendChild(hd);
        }
        var first = new Date(y, m, 1);
        first.setHours(12, 0, 0, 0);
        var padCount = mondayFirstIndex(first.getDay());
        var p;
        for (p = 0; p < padCount; p++) {
          var empty = document.createElement('div');
          empty.className = 'calendar-day calendar-day-empty';
          empty.setAttribute('aria-hidden', 'true');
          grid.appendChild(empty);
        }
        var daysInMonth = new Date(y, m + 1, 0).getDate();
        var today = new Date();
        today.setHours(0, 0, 0, 0);
        var d;
        for (d = 1; d <= daysInMonth; d++) {
          var cellDate = new Date(y, m, d);
          cellDate.setHours(12, 0, 0, 0);
          var phase = cellPhase(cellDate, lmp, cycleLen, plen, ci);
          var cell = document.createElement('div');
          cell.className = 'calendar-day';
          if (phase === 'o') cell.classList.add('calendar-day-ovulation');
          else if (phase === 'p') cell.classList.add('calendar-day-period');
          else if (phase === 'f') cell.classList.add('calendar-day-fertile');
          var todayN = new Date();
          todayN.setHours(0, 0, 0, 0);
          var cmp = new Date(y, m, d);
          cmp.setHours(0, 0, 0, 0);
          if (cmp.getTime() === todayN.getTime()) cell.classList.add('calendar-day-today');
          cell.textContent = String(d);
          cell.title = fmtLong(cellDate);
          grid.appendChild(cell);
        }
        var totalUsed = padCount + daysInMonth;
        var tail = (7 - (totalUsed % 7)) % 7;
        for (p = 0; p < tail; p++) {
          var empty2 = document.createElement('div');
          empty2.className = 'calendar-day calendar-day-empty';
          empty2.setAttribute('aria-hidden', 'true');
          grid.appendChild(empty2);
        }
        wrap.appendChild(grid);
        return wrap;
      }
      function run(resetCycleNav) {
        var lmpS = document.getElementById('poLmp').value;
        if (!lmpS) return;
        if (resetCycleNav) currentCycleIdx = 0;
        var lmp = new Date(lmpS + 'T12:00:00');
        var cycleLen = Math.min(45, Math.max(21, parseInt(document.getElementById('poCycle').value, 10) || 28));
        var plen = Math.min(10, Math.max(2, parseInt(document.getElementById('poLen').value, 10) || 5));
        var nc = parseInt(document.getElementById('poHorizon').value, 10) || 3;
        document.getElementById('poCycle').value = cycleLen;
        document.getElementById('poLen').value = plen;
        document.getElementById('poCycleR').value = cycleLen;
        document.getElementById('poLenR').value = plen;
        if (currentCycleIdx >= nc) currentCycleIdx = nc - 1;
        if (currentCycleIdx < 0) currentCycleIdx = 0;
        var ci = currentCycleIdx;
        var periodStart = addDays(lmp, ci * cycleLen);
        var nextCycleStart = addDays(lmp, (ci + 1) * cycleLen);
        var lastDayOfCycle = addDays(nextCycleStart, -1);
        document.getElementById('poCycleLabel').textContent = 'Cycle ' + (ci + 1) + ' of ' + nc;
        var summaryEl = document.getElementById('poCycleSummary');
        summaryEl.innerHTML = fmtCycleSummary(ci, lmp, cycleLen, plen);
        var statusEl = document.getElementById('poStatusCallout');
        statusEl.innerHTML = fmtStatusCallout(lmp, cycleLen, plen);
        statusEl.removeAttribute('hidden');
        document.getElementById('poPrev').disabled = ci <= 0;
        document.getElementById('poNext').disabled = ci >= nc - 1;
        var root = document.getElementById('poCalRoot');
        root.innerHTML = '';
        var monthList = monthsOverlappingCycle(periodStart, lastDayOfCycle);
        var i;
        for (i = 0; i < monthList.length; i++) {
          var ym = monthList[i];
          root.appendChild(buildMonthSection(ym.year, ym.month, lmp, cycleLen, plen, ci));
        }
        var ovX = addDays(nextCycleStart, -14);
        var edd = addDays(ovX, 266);
        document.getElementById('poDdp').innerHTML =
          'Rough due date ≈ <strong>' +
          fmtLong(edd) +
          '</strong> (from ovulation <strong>' +
          fmtLong(ovX) +
          '</strong>). Clinical dating may differ.';
      }
      function onInput() {
        run(true);
      }
      function goDelta(delta) {
        var lmpS = document.getElementById('poLmp').value;
        if (!lmpS) return;
        var nc = parseInt(document.getElementById('poHorizon').value, 10) || 3;
        currentCycleIdx = Math.max(0, Math.min(nc - 1, currentCycleIdx + delta));
        run(false);
      }
      var lmpEl = document.getElementById('poLmp');
      var d = new Date();
      d.setMonth(d.getMonth() - 1);
      lmpEl.value = fmt(d);
      ['poLmp', 'poCycle', 'poLen', 'poHorizon'].forEach(function (id) {
        document.getElementById(id).addEventListener('input', onInput);
        document.getElementById(id).addEventListener('change', onInput);
      });
      document.getElementById('poCycleR').addEventListener('input', function () {
        document.getElementById('poCycle').value = this.value;
        onInput();
      });
      document.getElementById('poLenR').addEventListener('input', function () {
        document.getElementById('poLen').value = this.value;
        onInput();
      });
      document.getElementById('poPrev').addEventListener('click', function () {
        goDelta(-1);
      });
      document.getElementById('poNext').addEventListener('click', function () {
        goDelta(1);
      });
      run(true);
    })();
  </script>

  <style>
    .breadcrumb { font-size: 13px; color: var(--text-muted); padding: 16px 0 0; max-width: 960px; }
    .breadcrumb a { color: var(--blue); text-decoration: none; }
    .hero { padding: 36px 0 16px; text-align: center; }
    .subtitle { font-size: 17px; color: var(--text-muted); max-width: 720px; margin: 0 auto 20px; line-height: 1.6; }
    .medical-callout {
      background: #fef2f2;
      border: 2px solid #f87171;
      color: var(--text);
      padding: 18px 20px;
      border-radius: 12px;
      font-size: 15px;
      line-height: 1.6;
      max-width: 900px;
      margin-bottom: 20px;
    }
    :global([data-theme='dark']) .medical-callout {
      background: rgba(248, 113, 113, 0.12);
      border-color: rgba(248, 113, 113, 0.5);
    }
    .medical-reviewed {
      font-size: 12px;
      color: var(--text-muted);
      margin-top: 8px;
      margin-bottom: 0;
    }
    .period-seo-section {
      padding: 28px 0;
    }
    .period-seo-section .container {
      max-width: 760px;
    }
    .period-seo-section h2 {
      font-size: 1.5rem;
      margin: 0 0 16px;
      color: var(--text);
      text-align: center;
    }
    .period-seo-section h3 {
      font-size: 1.05rem;
      margin: 22px 0 8px;
      color: var(--text);
    }
    .period-seo-section p {
      font-size: 15px;
      line-height: 1.75;
      color: var(--text-muted);
      margin: 0 0 12px;
    }
    .period-seo-list {
      margin: 0 0 16px;
      padding-left: 1.25rem;
      color: var(--text-muted);
      font-size: 15px;
      line-height: 1.65;
    }
    .period-seo-list li {
      margin-bottom: 8px;
    }
    .calc-section { padding: 16px 0 40px; }
    .calc-layout { display: grid; grid-template-columns: 1fr 1fr; gap: 28px; max-width: 960px; align-items: start; margin: 0 auto; }
    @media (max-width: 768px) { .calc-layout { grid-template-columns: 1fr; } }
    .calc-inputs, .calc-results { background: var(--card); border: 1px solid var(--border); border-radius: var(--radius-lg); padding: 22px 24px; box-shadow: var(--shadow); }
    .calc-heading { font-size: 1rem; margin: 0 0 16px; color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.04em; font-weight: 700; }
    .field label { display: block; font-weight: 600; margin-bottom: 6px; font-size: 14px; }
    .field input, .field select { width: 100%; padding: 10px 12px; border-radius: var(--radius-sm); border: 1px solid var(--border); font-size: 16px; }
    .sr-only {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      white-space: nowrap;
      border: 0;
    }
    .faq {
      padding: 24px 0 8px;
    }
    .faq .container {
      max-width: 820px;
    }
    .faq-item {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 20px 24px;
      margin-bottom: 12px;
    }
    .faq-item h3 {
      font-size: 16px;
      margin-bottom: 6px;
    }
    .faq-item p {
      font-size: 14px;
      color: var(--text-muted);
      margin: 0;
      line-height: 1.55;
    }
    .po-nav {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      margin-bottom: 14px;
    }
    .po-nav-btn {
      flex-shrink: 0;
      min-width: 44px;
      min-height: 44px;
      padding: 0 12px;
      border-radius: var(--radius-sm);
      border: 1px solid var(--border);
      background: var(--card);
      font-size: 1.2rem;
      line-height: 1;
      color: var(--text);
      cursor: pointer;
      transition: background 0.15s, border-color 0.15s;
    }
    .po-nav-btn:hover:not(:disabled) {
      background: var(--gray-100);
      border-color: var(--gray-300);
    }
    :global([data-theme='dark']) .po-nav-btn:hover:not(:disabled) {
      background: rgba(255, 255, 255, 0.08);
    }
    .po-nav-btn:disabled {
      opacity: 0.35;
      cursor: not-allowed;
    }
    .po-nav-btn:focus-visible {
      outline: 2px solid var(--blue);
      outline-offset: 2px;
    }
    .po-nav-label {
      font-size: 1.05rem;
      font-weight: 700;
      text-align: center;
      flex: 1;
      color: var(--text);
    }
    .po-cal-wrap {
      margin-top: 0;
    }
    .cycle-summary-host {
      width: 100%;
      margin-bottom: 8px;
    }
    .po-cal-root {
      width: 100%;
      max-width: 100%;
    }
    .po-ddp-wrap {
      margin-top: 18px;
      padding: 14px 16px;
      border-radius: var(--radius-md, 10px);
      background: rgba(59, 130, 246, 0.08);
      border: 1px solid rgba(59, 130, 246, 0.25);
    }
    :global([data-theme='dark']) .po-ddp-wrap {
      background: rgba(59, 130, 246, 0.12);
      border-color: rgba(96, 165, 250, 0.35);
    }
    .po-ddp-label {
      margin: 0 0 6px;
      font-size: 11px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.06em;
      color: var(--text-muted);
    }
    .po-ddp {
      font-size: 14px;
      color: var(--text);
      margin: 0;
      line-height: 1.55;
    }
    .po-ddp strong {
      font-weight: 700;
      font-variant-numeric: tabular-nums;
    }
    .edu { padding: 36px 0 48px; }
    .edu .container { max-width: 760px; }
    .edu p { font-size: 15px; line-height: 1.75; color: var(--text-muted); margin-bottom: 14px; }
    .edu a { color: var(--blue); font-weight: 600; }
    .section-title { text-align: center; margin-bottom: 18px; font-size: 1.5rem; }
    .faq { padding: 28px 0 32px; }
    .faq .container { max-width: 760px; }
    .faq-list { display: flex; flex-direction: column; gap: 12px; }
    .faq-item {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 16px 18px;
    }
    .faq-item h3 { margin: 0 0 8px; font-size: 15px; }
    .faq-item p { margin: 0; font-size: 14px; color: var(--text-muted); line-height: 1.55; }
    .related { text-align: center; padding: 8px 0 40px; }
    .related-guides {
      padding-top: 0;
      padding-bottom: 48px;
    }
    .related-list {
      text-align: left;
      max-width: 720px;
      margin: 0 auto;
      padding-left: 1.25rem;
      list-style: disc;
      color: var(--text-muted);
      font-size: 15px;
      line-height: 1.65;
    }
    .related-list li {
      margin-bottom: 12px;
    }
    .related-list a {
      color: var(--blue);
      font-weight: 600;
    }
    .related-links a { color: var(--blue); font-weight: 600; }
  </style>

  <!-- Global: JS injects .calendar-* nodes; scoped Astro CSS would not apply to them -->
  <style is:global>
    .calendar-legend {
      display: flex;
      gap: 16px;
      justify-content: center;
      margin-bottom: 16px;
      flex-wrap: wrap;
      font-size: 13px;
      color: var(--text-muted);
    }
    .legend-item {
      display: flex;
      align-items: center;
      gap: 6px;
    }
    .legend-dot {
      width: 12px;
      height: 12px;
      border-radius: 3px;
      display: inline-block;
      flex-shrink: 0;
    }
    .legend-period {
      background: #fee2e2;
      border: 1px solid #b91c1c;
    }
    .legend-fertile {
      background: #d1fae5;
      border: 1px solid #047857;
    }
    .legend-ovulation {
      background: #10b981;
      border: 1px solid #047857;
    }
    .cycle-status-callout {
      background: rgba(59, 130, 246, 0.1);
      border: 1px solid rgba(59, 130, 246, 0.28);
      padding: 16px 20px;
      border-radius: 12px;
      margin-bottom: 18px;
      box-sizing: border-box;
    }
    [data-theme='dark'] .cycle-status-callout {
      background: rgba(59, 130, 246, 0.15);
      border-color: rgba(96, 165, 250, 0.35);
    }
    .cycle-status-line {
      margin: 0 0 10px;
      font-size: 14px;
      line-height: 1.55;
      color: var(--text);
    }
    .cycle-status-line:last-child {
      margin-bottom: 0;
    }
    .cycle-status-line--main {
      font-size: 15px;
    }
    .cycle-status-line--chance {
      font-size: 13px;
      color: var(--text-muted);
    }
    .cycle-status-hint {
      font-size: 12px;
      font-weight: 400;
      opacity: 0.9;
    }
    .cycle-summary {
      margin: 0 0 20px;
    }
    .cycle-summary-title {
      font-size: 1.05rem;
      font-weight: 700;
      margin: 0 0 14px;
      text-align: center;
      color: var(--text);
      line-height: 1.35;
    }
    .cycle-stats {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 10px;
    }
    @media (min-width: 520px) {
      .cycle-stats {
        grid-template-columns: repeat(4, 1fr);
      }
    }
    .stat {
      background: var(--gray-50);
      border: 1px solid var(--border);
      border-radius: 10px;
      padding: 12px 10px;
      display: flex;
      flex-direction: column;
      gap: 4px;
      text-align: center;
      box-sizing: border-box;
      min-width: 0;
    }
    [data-theme='dark'] .stat {
      background: rgba(255, 255, 255, 0.04);
    }
    .stat-highlight {
      border-color: rgba(16, 185, 129, 0.45);
      background: rgba(16, 185, 129, 0.1);
    }
    [data-theme='dark'] .stat-highlight {
      background: rgba(16, 185, 129, 0.15);
      border-color: rgba(52, 211, 153, 0.4);
    }
    .stat-label {
      font-size: 10px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.06em;
      color: var(--text-muted);
    }
    .stat-value {
      font-size: 14px;
      font-weight: 600;
      color: var(--text);
      line-height: 1.35;
      word-break: break-word;
    }
    .stat-detail {
      font-size: 12px;
      color: var(--text-muted);
    }
    .calendar-month {
      margin-bottom: 32px;
    }
    .calendar-month:last-child {
      margin-bottom: 0;
    }
    .calendar-month-title {
      text-align: center;
      font-size: 18px;
      font-weight: 600;
      margin: 0 0 16px;
      color: var(--text);
    }
    .calendar-grid {
      display: grid;
      grid-template-columns: repeat(7, 1fr);
      gap: 4px;
      background: var(--card);
      padding: 12px;
      border-radius: 12px;
      border: 1px solid var(--border);
      width: 100%;
      max-width: 420px;
      margin: 0 auto;
      box-sizing: border-box;
    }
    .calendar-day-header {
      text-align: center;
      padding: 8px 4px;
      font-size: 12px;
      font-weight: 600;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    .calendar-day {
      aspect-ratio: 1 / 1;
      min-height: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 14px;
      font-weight: 500;
      border-radius: 8px;
      background: transparent;
      color: var(--text);
      position: relative;
      cursor: default;
      transition: background 0.15s ease;
      box-sizing: border-box;
    }
    .calendar-day:hover:not(.calendar-day-empty) {
      background: var(--gray-100);
    }
    .calendar-day-empty {
      background: transparent !important;
      pointer-events: none;
    }
    .calendar-day-period {
      background: #fee2e2;
      color: #b91c1c;
      font-weight: 600;
    }
    .calendar-day-fertile {
      background: #d1fae5;
      color: #047857;
    }
    .calendar-day-ovulation {
      background: #10b981;
      color: white;
      font-weight: 700;
      box-shadow: 0 0 0 2px var(--card), 0 0 0 4px #10b981;
    }
    .calendar-day-today {
      border: 2px solid var(--blue);
    }
    [data-theme='dark'] .calendar-day:hover:not(.calendar-day-empty) {
      background: rgba(255, 255, 255, 0.06);
    }
    [data-theme='dark'] .calendar-day-period {
      background: rgba(220, 38, 38, 0.2);
      color: #fca5a5;
    }
    [data-theme='dark'] .calendar-day-fertile {
      background: rgba(16, 185, 129, 0.2);
      color: #6ee7b7;
    }
    [data-theme='dark'] .calendar-day-ovulation {
      box-shadow: 0 0 0 2px var(--card), 0 0 0 4px #10b981;
    }
    @media (max-width: 480px) {
      .calendar-grid {
        gap: 2px;
        padding: 8px;
        max-width: 100%;
      }
      .calendar-day {
        font-size: 12px;
      }
      .calendar-day-header {
        font-size: 10px;
        padding: 6px 2px;
      }
      .calendar-month-title {
        font-size: 16px;
      }
    }
  </style>
</MainLayout>
